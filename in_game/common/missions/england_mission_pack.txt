england_mission_pack = {
	icon = generic_capital_economy
	chance = 3600
	repeatable = no
	player_playstyle = diplomatic

	ai_will_do = { value = 100 }

	visible = {
		game_has_missions_enabled = yes
		OR = {
			tag = "ENG"
			tag = "GBR"
		}
	}

	on_start = {
		set_variable = {
			name = mission_target_trade_income
			value = 75
		}
		set_variable = {
			name = mission_target_parliament_support
			value = 70
		}
		set_variable = {
			name = mission_target_cultural_tradition
			value = 50
		}
	}

	on_completion = {
		add_prestige = 100
		add_legitimacy = 100
		remove_variable = mission_target_trade_income
		remove_variable = mission_target_parliament_support
		remove_variable = mission_target_cultural_tradition
	}

	on_abort = {
		remove_variable = mission_target_trade_income
		remove_variable = mission_target_parliament_support
		remove_variable = mission_target_cultural_tradition
	}

	# ==================== PHASE 1: ENGLISH CONSOLIDATION ====================
	
	mission_secure_the_english_crown = {
		icon = crown_icon
		requires = { }
		
		enabled = {
			stability >= 30
			legitimacy >= 50
			# NOT = { has_country_modifier = war_of_the_roses }
			owns = location:london
			owns = location:winchester
			"estate_power(estate_type:crown_estate)" >= 0.2
		}
		
		duration = 0

		on_completion = {
			# add_country_modifier = {
			# 	modifier = secure_english_monarchy
			# 	years = 10
			# 	mode = add
			# }
			add_legitimacy = 25
			trigger_event_silently = england_mission_events.1
		}
		ai_will_do = { value = 100 }
	}

	mission_develop_london = {
		icon = cultural_acceptance_advance
		requires = { mission_secure_the_english_crown }
		
		enabled = {
			capital = {
				development >= 40
				total_building_levels >= 40
				has_building_with_at_least_one_level = marketplace
			}
			monthly_income_trade_and_tax >= 75
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = london_thriving_capital
					years = 12
					mode = add
				}
				change_development = 4
			}
			trigger_event_silently = england_mission_events.2
		}
		ai_will_do = { value = 100 }
	}

	mission_control_english_channel = {
		icon = maritime_advance_age_2
		requires = {  }
		
		enabled = {
			total_effective_building_levels:wharf >= 8
			navy_size >= 30
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = channel_control
				years = 12
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_establish_wool_trade = {
		icon = merchants_and_trade
		requires = { mission_secure_the_english_crown }
		
		enabled = {
			any_owned_location = {
				raw_material = goods:wool
				count >= 20
			}
			any_owned_location = {
				# has_building_with_at_least_one_level = cloth_workshop #it is a tier4 building so maybe for a later mission
				has_building_with_at_least_one_level = cloth_guild
				count >= 18
			}
			produced_in_country:cloth >= 32
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = english_wool_industry
				years = 25
				mode = add
			}
			add_estate_satisfaction = {
				type = estate_type:burghers_estate
				value = estate_satisfaction_mild_bonus
			}
			trigger_event_silently = england_mission_events.3
		}
		ai_will_do = { value = 100 }
	}

	mission_summon_model_parliament = {
		icon = government_reforms
		requires = { mission_secure_the_english_crown }
		
		enabled = {
			has_parliament = yes
			estate_satisfaction:nobles_estate >= 0.65
			estate_satisfaction:burghers_estate >= 0.60
			stability >= 55
		}
		
		duration = 0

		on_completion = {
			trigger_event_non_silently = england_mission_events.4
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 2: BRITISH ISLES EXPANSION ====================
	
	mission_subjugate_wales = {
		icon = offensive_army
		requires = { mission_secure_the_english_crown }
		
		enabled = {
			area:wales_area = {
				any_location_in_area = {
					percent >= 0.9
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = welsh_integration
				years = 15
				mode = add
			}
			add_manpower = {
				value = max_manpower
				multiply = 0.1
			}
			trigger_event_silently = england_mission_events.5
		}
		ai_will_do = { value = 100 }
	}

	mission_develop_cornwall = {
		icon = glorious_arms
		requires = { mission_secure_the_english_crown }
		
		enabled = {
			province:devon_province = {			
				any_location_in_province = {
					raw_material = goods:tin
					count >= 3
				}
			}
			produced_in_country:tin >= 28
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = cornish_tin_mines
				years = 10
				mode = add
			}
			add_gold = 200
		}
		ai_will_do = { value = 100 }
	}

	mission_pacify_the_north = {
		icon = defensive_army
		requires = { mission_subjugate_wales }
		
		enabled = {
			area:northumbria_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.9
				}
			}
			area:northumbria_area = {
				any_location_in_area = {          
					custom_tooltip = {
						text = is_owner_generic_check
						owner ?= root
					}
					has_building_with_at_least_one_level = castle
					count >= 6
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = northern_border_secure
				years = 10
				mode = add
			}
			add_stability = 10
			trigger_event_silently = england_mission_events.6
		}
		ai_will_do = { value = 100 }
	}

	mission_invade_ireland = {
		icon = glorious_arms
		requires = { mission_pacify_the_north }
		
		enabled = {
			area:leinster_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.6
				}
			}
			area:munster_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.6
				}
			}
		}
		
		duration = 0

		on_completion = {
			#Add claims to Irish locations (using pattern from official code)
			location:dublin = {
				add_core = root
			}
			location:new_ross = {
				add_core = root
			}
			location:cork = {
				add_core = root
			}
			location:clonmel = {
				add_core = root
			}
			location:ferns = {
				add_core = root
			}
			
			add_country_modifier = {
				modifier = irish_expansion
				years = 15
				mode = add
			}
			trigger_event_silently = england_mission_events.7
		}
		ai_will_do = { value = 100 }
	}

	mission_complete_irish_conquest = {
		icon = offensive_army
		requires = { mission_invade_ireland }
		
		enabled = {
			region:ireland_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.9
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = lordship_of_ireland
				years = 20
				mode = add
			}

			trigger_event_silently = england_mission_events.8
		}
		ai_will_do = { value = 100 }
	}

	mission_claim_scottish_throne = {
		icon = crown_icon
		requires = { mission_invade_ireland }
		
		enabled = {
			root = {
				relative_strength = {
					target = c:SCO
					value > 0.9
				}
			}

			prestige >= 30
			legitimacy >= 75
		}
		
		duration = 0

		on_completion = {
			if = {
				limit = {
					NOT = { c:SCO ?= { is_subject_of = root } }
				}
				add_casus_belli = {
					target ?= c:SCO
					type = casus_belli:cb_subjugation
					years = 20
				}
			}

			add_stability = 30
			trigger_event_silently = england_mission_events.9
		}
		ai_will_do = { value = 100 }
	}

	mission_unite_british_isles = {
		icon = empire_icon
		requires = { mission_claim_scottish_throne mission_complete_irish_conquest }
		
		enabled = {
			region:great_britain_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.95
				}
			}
			prestige >= 60
			legitimacy >= 90
			monthly_income_trade_and_tax >= 250
		}
		
		duration = 0

		on_completion = {
			# Add cores to all British Isles locations

			custom_tooltip = {
				text = any_location_in_britain_region_becomes_core
				region:great_britain_region = {
					every_location_in_region = {
						limit = { owner ?= root }
						add_core = root
					}
				}
			}			
			add_country_modifier = {
				modifier = british_isles_unified
				years = 40
				mode = add
			}
			add_stability = 30
			trigger_event_silently = england_mission_events.10
		}
		ai_will_do = { value = 100 }
	}

	mission_form_great_britain = {
		icon = empire_icon
		requires = { mission_unite_british_isles }
		
		enabled = {
			has_or_had_tag = GBR

			average_control_in_home_region >= 0.4
			# owns = location:london
			# owns = location:edinburgh
			# owns = location:dublin
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = kingdom_of_great_britain
				years = -1
				mode = add
			}
			trigger_event_silently = england_mission_events.11
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 3: FRENCH WARS BRANCH ====================
	
	mission_secure_calais = {
		icon = defensive_army
		requires = { mission_secure_the_english_crown }
		
		enabled = {
			location:calais = {
				owner ?= root
				has_building_with_at_least_one_level = castle
			}
		}
		
		duration = 0

		on_completion = {
			location:calais = {
				add_location_modifier = {
					modifier = english_bridgehead
					years = 15
					mode = add
				}
				add_core = root
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_claim_french_throne = {
		icon = crown_icon
		requires = { mission_secure_calais }
		
		enabled = {
			country_exists = c:FRA
			is_situation_active = situation:hundred_years_war
			army_size_percentage >= 0.9
		}
		
		duration = 0

		on_completion = {
			# works but we add this cb with the event
			# add_casus_belli = {
			# 	target = c:FRA
			# 	type = casus_belli:cb_hundred_years_war
			# 	months = 120
			# }
			# add_country_modifier = {
			# 	modifier = claim_to_french_throne
			# 	years = 10
			# 	mode = add
			# }
			trigger_event_non_silently = england_mission_events.12
		}
		ai_will_do = { value = 100 }
	}

	mission_conquer_normandy = {
		icon = offensive_army
		requires = { mission_claim_french_throne }
		
		enabled = {
			area:normandy_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.75
				}
			}
		}
		
		duration = 0

		on_completion = {			
			add_country_modifier = {
				modifier = norman_heritage_restored
				years = 15
				mode = add
			}
			add_prestige = 10
			trigger_event_silently = england_mission_events.13
		}
		ai_will_do = { value = 100 }
	}

	mission_control_gascony = {
		icon = glorious_arms
		requires = { mission_claim_french_throne }
		
		enabled = {
			area:gascony_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.75
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = gacony_restored
				years = 15
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_conquer_guyenne = {
		icon = offensive_army
		requires = { mission_control_gascony }
		
		enabled = {
			area:guyenne_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.75
				}
			}
			area:guyenne_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					raw_material = goods:wine
					count >= 4
				}
			}
		}
		
		duration = 0

		on_completion = {			
			add_country_modifier = {
				modifier = guyenne_wine_trade
				years = 15
				mode = add
			}
			add_prestige = 10

			area:guyenne_area = {
				every_location_in_area = {
					limit = { owner ?= root }
					add_core = root
				}
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_win_hundred_years_war = {
		icon = empire_icon
		requires = { mission_conquer_normandy mission_conquer_guyenne }
		
		enabled = {
			OR = {
				NOT = { country_exists = c:FRA }
				c:FRA = {
					OR = {
						is_subject_of = root
					}
				}
			}
			owns = location:paris
			NOT = {
				is_situation_active = situation:hundred_years_war
			}

		}
		
		duration = 0

		on_completion = {
			# Add cores to key French locations
			location:paris = {
				add_core = root
			}
			location:orleans = {
				add_core = root
			}
			
			add_country_modifier = {
				modifier = dual_monarchy_established
				years = -1
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 4: NAVAL SUPREMACY BRANCH ====================
	
	mission_build_royal_navy = {
		icon = ship_building_advance
		requires = { mission_control_english_channel }
		
		enabled = {
			navy_size >= 50
			OR = {
				total_effective_building_levels:dock >= 6
				total_effective_building_levels:dry_dock >= 4
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = royal_navy_established
				years = 20
				mode = add
			}
			add_navy_tradition = 15
			trigger_event_silently = england_mission_events.15
		}
		ai_will_do = { value = 100 }
	}

	mission_develop_portsmouth = {
		icon = production_efficiency_advance
		requires = { mission_build_royal_navy }
		
		enabled = {
			location:portsmouth = {
				owner ?= root
				development >= 30
				OR = {
					has_building_with_at_least_one_level = dry_dock
					has_building_with_at_least_one_level = shipyard
				}
			}
		}
		
		duration = 0

		on_completion = {
			location:portsmouth = {
				add_location_modifier = {
					modifier = premier_naval_base
					years = -1
					mode = add
				}
				change_development = 2
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_dominate_north_sea = {
		icon = maritime_advance_age_2
		requires = { mission_build_royal_navy mission_develop_london }
		
		enabled = {
			navy_tradition >= 20
			navy_size >= 80
		}
	
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = north_sea_dominance
				years = 20
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_establish_royal_marines = {
		icon = glorious_arms
		requires = { mission_develop_portsmouth mission_dominate_north_sea }
		
		enabled = {
			navy_tradition >= 50
			army_tradition >= 50
			any_owned_location = {
				# is_coastal = yes
				has_building_with_at_least_one_level = barracks
				count >= 8
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = royal_marines_established
				years = -1
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_control_gibraltar = {
		icon = ship_building_advance
		requires = { mission_dominate_north_sea }
		
		enabled = {
			location:gibraltar = {
				owner ?= root
				has_building_with_at_least_one_level = castle
			}
		}
		
		duration = 0

		on_completion = {
			location:gibraltar = {
				add_location_modifier = {
					modifier = key_to_mediterranean
					years = -1
					mode = add
				}
				add_core = root
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_secure_suez = {
		icon = ship_building_advance
		requires = { mission_control_gibraltar }
		
		enabled = {
			location:suez = {
				owner ?= root
			}

			region:egypt_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					OR = {
						has_building_with_at_least_one_level = dock
						has_building_with_at_least_one_level = dry_dock
						has_building_with_at_least_one_level = shipyard
					}
					count >= 3
				}
			}

		}
		
		duration = 0

		on_completion = {
			location:suez = {
				add_location_modifier = {
					modifier = suez_canal_control
					years = -1
					mode = add
				}
				add_core = root
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_rule_the_waves = {
		icon = empire_icon
		requires = { mission_control_gibraltar mission_secure_suez }
		
		enabled = {
			navy_tradition >= 75
			navy_size >= 200
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = ruler_of_the_waves
				years = 30
				mode = add
			}
			trigger_event_silently = england_mission_events.17
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 5: TRADE AND ECONOMY BRANCH ====================
	
	mission_establish_merchant_companies = {
		icon = merchants_and_trade
		requires = { mission_establish_wool_trade }
		
		enabled = {
			total_effective_building_levels:merchants_quarters >= 10
			monthly_income_trade_and_tax >= var:mission_target_trade_income
			capital.market = {
				"merchant_power_in_market(root)" >= 70
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = merchant_companies_chartered
				years = 25
				mode = add
			}
			add_gold = {
			value = {
				add = monthly_income_trade_and_tax
				multiply = 5
			}
		}
			trigger_event_silently = england_mission_events.18
		}
		ai_will_do = { value = 100 }
	}

	mission_develop_banking = {
		icon = court_accounting
		requires = { mission_establish_merchant_companies }
		
		enabled = {
			has_advance = banking_advance
			# cant build bank_office natural it is granted with events so we grand one
			# capital = {
			# 	has_building_with_at_least_one_level = banking_office
			# }
			monthly_income_trade_and_tax >= 100
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = english_banking_system
				years = 20
				mode = add
			}

			location:london = {
				change_building_level_in_location = {
					building = building_type:banking_office
					value = 1
					owner ?= root
				}
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_control_baltic_trade = {
		icon = trade_efficiency
		requires = { mission_establish_wool_trade }
		
		enabled = {
			region:baltic_region = {
				any_location_in_region = {
						OR = {
							custom_tooltip = {
									text = is_owner_generic_check
									owner ?= root
							}
							custom_tooltip = {
									text = is_owner_generic_subject_check
									owner ?= { is_subject_of = root }
							}
						}
					
					count >= 20
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = baltic_trade_dominance
				years = 20
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_monopolize_spice_trade = {
		icon = merchants_and_trade
		requires = { mission_control_baltic_trade }
		
		enabled = {
			any_owned_location = {
				raw_material = goods:chili
				count >= 6
			}
			produced_in_country:chili >= 30
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = spice_trade_monopoly_eng
				years = 25
				mode = add
			}
			add_estate_satisfaction = {
				type = estate_type:burghers_estate
				value = estate_satisfaction_severe_bonus
			}
			trigger_event_non_silently = england_mission_events.19
		}
		ai_will_do = { value = 100 }
	}

	mission_establish_london_stock_exchange = {
		icon = production_efficiency_advance
		requires = { mission_develop_banking mission_monopolize_spice_trade }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = stock_exchange
				development >= 50
			}
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = financial_capital_of_world
					years = 40
					mode = add
				}
			}
			trigger_event_silently = england_mission_events.20
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 6: COLONIAL EXPANSION BRANCH ====================
	
	mission_sponsor_exploration = {
		icon = ship_building_advance
		requires = { mission_control_english_channel }
		
		enabled = {
			any_character = {
				# has_trait = explorer
				has_trait_category = explorer
			}
			gold >= 2500
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = age_of_exploration
				years = 40
				mode = add
			}
			create_character = {
				first_name = name_john
				last_name = Cabot
				adm = { 60 80 }
				dip = { 70 90 }
				mil = { 50 70 }
				# normally this character is born way later
				# birth_date = 1450.1.1
				age = 34
				culture = culture:english
			}
			add_gold = -3000
		}
		ai_will_do = { value = 100 }
	}

	mission_establish_colonial_office = {
		icon = government_reforms
		requires = { mission_sponsor_exploration }
		
		enabled = {
			num_subjects >= 10
			any_subject = {
				is_subject_type = colonial_nation
				count >= 5
			}
			#checking for colonial nations is enough for now
			# monthly_income_total >= 150
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = colonial_administration
				years = 35
				mode = add
			}
			every_subject = {
				limit = { is_subject_type = colonial_nation }
				add_liberty_desire = -25
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_discover_the_americas = {
		icon = ship_building_advance
		requires = { mission_sponsor_exploration }
		duration = 0
		enabled = {
				OR = {
						has_discovered_area = area:canadian_atlantic_coast_area
						has_discovered_area = area:east_coast_sea_zones_area
						has_discovered_area = area:central_america_sea_zones_area
				}
		}
		on_completion = {
			add_country_modifier = {
				modifier = unknown_frontier_modifier
				years = 25
				mode = add
			}
		}
	}

	mission_colonize_north_america = {
		icon = ship_building_advance
		requires = { mission_discover_the_americas }
		
		enabled = {
			region:canada_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.20
				}
			}
			region:east_coast_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.15
				}
			}
			any_subject = {
				is_subject_type = colonial_nation
				count >= 2
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = thirteen_colonies_established
				years = 30
				mode = add
			}
			add_gold = 1200
		}
		ai_will_do = { value = 100 }
	}

	mission_secure_caribbean = {
		icon = ship_building_advance
		requires = { mission_sponsor_exploration }
		
		enabled = {
			region:caribbean_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					count >= 15
				}
			}
			region:caribbean_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					raw_material = goods:tobacco
					count >= 4
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = caribbean_tobacco_empire
				years = 25
				mode = add
			}
			add_gold = 600
		}
		ai_will_do = { value = 100 }
	}

	mission_establish_east_india_company = {
		icon = merchants_and_trade
		requires = { mission_establish_colonial_office }
		
		enabled = {
			region:deccan_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					count >= 25
				}
			}
			region:deccan_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					count >= 3
					has_building_with_at_least_one_level = merchants_quarters
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = east_india_company_charter
				years = 40
				mode = add
			}
			trigger_event_silently = england_mission_events.23
		}
		ai_will_do = { value = 100 }
	}

	mission_gb_sail_around_africa = {
		icon = ship_building_advance
		requires = { mission_sponsor_exploration }
		duration = 0
		enabled = {
				has_discovered_area = area:goringhaikona_area
		}
		on_completion = {
				if = {
						limit = {
								location:goringhaikona = {
										is_ownable = yes
										NOT = { has_owner = yes }
								}
						}
						location:goringhaikona = {
								change_location_owner ?= root
								add_core = root
						}
				}
				else = {
						area:goringhaikona_area = {
								random_location_in_area = {
										limit = {
												is_ownable = yes
												NOT = { has_owner = yes }
												is_coastal = yes
										}
										change_location_owner ?= root
										add_core = root
								}
						}
				}
		}
	}

	mission_secure_cape_colony = {
		icon = ship_building_advance
		requires = { mission_gb_sail_around_africa }
		
		enabled = {
			area:goringhaikona_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					count >= 10
				}
			}
			area:goringhaikona_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					count >= 3
					OR = {
						has_building_with_at_least_one_level = dry_dock
						has_building_with_at_least_one_level = shipyard
					}
				}
			}
		}
		duration = 0

		on_completion = {
			area:goringhaikona_area = {
				random_location_in_area = {
					limit = {
						OR = {
							owner ?= root
							owner ?= { is_subject_of = root }
						}
					}
					add_location_modifier = {
						modifier = vital_supply_station
						years = -1
						mode = add
					}
					add_core = root
				}
			}
		}
		ai_will_do = { value = 100 }
	}
	

	mission_colonize_australia = {
		icon = ship_building_advance
		requires = { mission_sponsor_exploration }
		
		enabled = {
			region:australia_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					count >= 25
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = australian_penal_colonies
				years = 30
				mode = add
			}
			add_prestige = 10
		}
		ai_will_do = { value = 100 }
	}

	mission_empire_where_sun_never_sets = {
		icon = empire_icon
		requires = { 
			mission_secure_cape_colony
			mission_colonize_australia
			mission_establish_east_india_company
		}
		
		enabled = {
			is_great_power = yes
			prestige >= 75
			num_subjects >= 15
			total_development >= 3000
			
			# Presence on all continent
			#these checks works but are laggy
			# continent:europe = {
			# 	any_location_in_continent = {
			# 		OR = {
			# 			custom_tooltip = {
			# 					text = is_owner_generic_check
			# 					owner ?= root
			# 			}
			# 			custom_tooltip = {
			# 					text = is_owner_generic_subject_check
			# 					owner ?= { is_subject_of = root }
			# 			}
			# 		}
			# 		count >= 100
			# 	}
			# }
			# continent:asia = {
			# 	any_location_in_continent = {
			# 		OR = {
			# 			custom_tooltip = {
			# 					text = is_owner_generic_check
			# 					owner ?= root
			# 			}
			# 			custom_tooltip = {
			# 					text = is_owner_generic_subject_check
			# 					owner ?= { is_subject_of = root }
			# 			}
			# 		}
			# 		count >= 50
			# 	}
			# }
			# continent:africa = {
			# 	any_location_in_continent = {
			# 		OR = {
			# 			custom_tooltip = {
			# 					text = is_owner_generic_check
			# 					owner ?= root
			# 			}
			# 			custom_tooltip = {
			# 					text = is_owner_generic_subject_check
			# 					owner ?= { is_subject_of = root }
			# 			}
			# 		}
			# 		count >= 30
			# 	}
			# }
			# continent:america = {
			# 	any_location_in_continent = {
			# 		OR = {
			# 			custom_tooltip = {
			# 					text = is_owner_generic_check
			# 					owner ?= root
			# 			}
			# 			custom_tooltip = {
			# 					text = is_owner_generic_subject_check
			# 					owner ?= { is_subject_of = root }
			# 			}
			# 		}
			# 		count >= 200
			# 	}
			# }
			# continent:oceania = {
			# 	any_location_in_continent = {
			# 		OR = {
			# 			custom_tooltip = {
			# 					text = is_owner_generic_check
			# 					owner ?= root
			# 			}
			# 			custom_tooltip = {
			# 					text = is_owner_generic_subject_check
			# 					owner ?= { is_subject_of = root }
			# 			}
			# 		}
			# 		count >= 25
			# 	}
			# }
			region:canada_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					count >= 25
				}
			}
			region:southern_africa_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					count >= 25
				}
			}
			region:deccan_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					count >= 25
				}
			}
			region:australia_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
								text = is_owner_generic_check
								owner ?= root
						}
						custom_tooltip = {
								text = is_owner_generic_subject_check
								owner ?= { is_subject_of = root }
						}
					}
					count >= 25
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = sun_never_sets_on_british_empire
				years = -1
				mode = add
			}

			add_legitimacy = 75
			trigger_event_non_silently = england_mission_events.24
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 7: RELIGIOUS REFORMATION BRANCH ====================
	
	mission_break_with_rome = {
		icon = religious_icon
		requires = { mission_summon_model_parliament }
		
		enabled = {
			# Safe religion check
			OR = {
				religion ?= religion:anglican
				religion ?= religion:lutheran
			}

			religious_unity >= 0.7
			"estate_power(estate_type:clergy_estate)" <= 0.25
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = english_reformation
				years = 25
				mode = add
			}
			trigger_event_silently = england_mission_events.25
		}
		ai_will_do = { value = 100 }
	}

	mission_dissolve_monasteries = {
		icon = government_reforms
		requires = { mission_break_with_rome }
		
		enabled = {
			any_owned_location = {
				has_building_with_at_least_one_level = monastery
				count <= 5
			}
			gold >= 1500
		}
		
		duration = 0

		on_completion = {

			ordered_owned_location = {
				limit = {
					NOT = { has_building_with_at_least_one_level = monastery }
					owner ?= root
				}
				order_by = development
				max = 5
				change_prosperity = 0.2
				change_development = 3
			}

		}
		ai_will_do = { value = 100 }
	}

	mission_promote_protestantism = {
		icon = diplomatic_influence
		requires = { mission_dissolve_monasteries }
		
		enabled = {
			any_country = {
				# Safe religion check with custom tooltip
				OR = {
					custom_tooltip = {
						text = is_religion_protestant_check
						religion ?= religion:lutheran
					}
					custom_tooltip = {
						text = is_religion_anglican_check
						religion ?= religion:lutheran
					}
				}

				# is_allied_with = { target = scope:actor }
				opinion = {
					target = root
					value >= 75
				}
				count >= 5
			}
			religious_influence >= 70
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = protestant_leadership
				years = 35
				mode = add
			}
			trigger_event_silently = england_mission_events.27
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 8: GOVERNMENT REFORMS BRANCH ====================
	
	mission_pass_magna_carta = {
		icon = bill_of_rights
		requires = { mission_summon_model_parliament }
		
		enabled = {
			"estate_power(estate_type:crown_estate)" >= 0.25
			"estate_power(estate_type:nobles_estate)" <= 0.45
			legitimacy >= 80
			stability >= 65
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = magna_carta_tradition
				years = 25
				mode = add
			}

			trigger_event_silently = england_mission_events.28
		}
		ai_will_do = { value = 100 }
	}

	mission_establish_common_law = {
		icon = government_reforms
		requires = { mission_pass_magna_carta }
		
		enabled = {
			average_country_literacy >= 25
			total_effective_building_levels:library >= 7
			"estate_power(estate_type:nobles_estate)" <= 0.40
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = english_common_law
				years = 35
				mode = add
			}
			add_research_progress = research_progress_mild_bonus
		}
		ai_will_do = { value = 100 }
	}

	mission_glorious_revolution = {
		icon = government_reforms
		requires = { mission_establish_common_law }
		
		enabled = {
			has_parliament = yes
			"estate_power(estate_type:crown_estate)" >= 0.35
			legitimacy >= 90
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = glorious_revolution
				years = 20
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 9: CULTURAL AND EDUCATIONAL BRANCH ====================
	
	mission_promote_english_culture = {
		icon = cultural_acceptance_advance
		requires = { mission_secure_the_english_crown }
		
		enabled = {
			culture = culture:english
			total_effective_building_levels:library >= 3
			culture:english = {
				cultural_tradition >= 400
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = english_cultural_renaissance
				years = 20
				mode = add
			}
			trigger_event_silently = england_mission_events.31
		}
		ai_will_do = { value = 100 }
	}

	mission_found_oxford_university = {
		icon = technology_icon
		requires = { mission_promote_english_culture }
		
		enabled = {
			location:oxford = {
				has_building_with_at_least_one_level = university
				development >= 35
			}
			average_country_literacy >= 20
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = oxford_university
				years = -1
				mode = add
			}
			trigger_event_silently = england_mission_events.32
		}
		ai_will_do = { value = 100 }
	}

	mission_establish_cambridge_colleges = {
		icon = technology_icon
		requires = { mission_found_oxford_university }
		
		enabled = {
			location:cambridge = {
				has_building_with_at_least_one_level = university
				development >= 30
			}
			total_effective_building_levels:university >= 7
			average_country_literacy >= 25
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = oxbridge_tradition
				years = 30
				mode = add
			}
			add_research_progress = research_progress_extreme_bonus
		}
		ai_will_do = { value = 100 }
	}

	mission_shakespearean_theater = {
		icon = cultural_acceptance_advance
		requires = { mission_promote_english_culture }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = theater
				development >= 40
			}
			culture:english = {
				cultural_tradition >= 75
			}
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = globe_theatre
					years = -1
					mode = add
				}
			}
			culture:english = {
				add_cultural_tradition = 15
			}
			trigger_event_silently = england_mission_events.33
		}
		ai_will_do = { value = 100 }
	}

	mission_establish_royal_society = {
		icon = research_icon
		requires = { mission_establish_cambridge_colleges }
		
		enabled = {
			average_country_literacy >= 30
			total_effective_building_levels:university >= 15
			has_embraced_institution = institution:scientific_revolution
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = royal_society_established
				years = -1
				mode = add
			}

			trigger_event_silently = england_mission_events.34
		}
		ai_will_do = { value = 100 }
	}

	mission_promote_english_literature = {
		icon = cultural_acceptance_advance
		requires = { mission_shakespearean_theater }
		
		enabled = {
			has_embraced_institution = institution:printing_press
			total_effective_building_levels:library >= 12
			average_country_literacy >= 35
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = english_literary_tradition
				years = -1
				mode = add
			}
			add_prestige = 30
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 10: MILITARY REFORMS BRANCH ====================
	
	mission_establish_standing_army = {
		icon = medieval_military
		requires = { mission_secure_the_english_crown }
		
		enabled = {
			army_size_percentage >= 0.95
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = standing_army_established
				years = 15
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_develop_professional_army = {
		icon = glorious_arms
		requires = { mission_establish_standing_army }
		
		enabled = {
			any_owned_location = {
				has_building_with_at_least_one_level = armory
				count >= 3
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = english_longbowmen
				years = 20
				mode = add
			}
			trigger_event_silently = england_mission_events.35
		}
		ai_will_do = { value = 100 }
	}

	mission_modernize_artillery = {
		icon = artillery_icon
		requires = { mission_establish_standing_army }
		
		enabled = {
			any_owned_location = {
				raw_material = goods:iron
				count >= 13
			}
			any_owned_location = {
				raw_material = goods:copper
				count >= 8
			}
			any_owned_location = {
				has_building_with_at_least_one_level = cannon_maker
				count >= 5
			}
			has_embraced_institution = institution:artillery_institution
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = modern_artillery_corps
				years = 35
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_redcoat_regiments = {
		icon = offensive_army
		requires = { mission_develop_professional_army mission_modernize_artillery }
		
		enabled = {
			army_tradition >= 50
			any_owned_location = {
				has_building_with_at_least_one_level = barracks
				count >= 15
			}
			#@@@ maybe a big number
			army_size >= 50
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = redcoat_regiments
				years = 30
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 11: INDUSTRIAL REVOLUTION BRANCH ====================
	
	mission_develop_coal_mining = {
		icon = expanded_supply_trains
		requires = { mission_develop_london }
		
		enabled = {
			any_owned_location = {
				raw_material = goods:coal
				count >= 15
			}
			produced_in_country:coal >= 25
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = coal_mining_industry
				years = 25
				mode = add
			}
			trigger_event_silently = england_mission_events.37
		}
		ai_will_do = { value = 100 }
	}

	mission_develop_iron_industry = {
		icon = production_efficiency_advance
		requires = { mission_develop_london }
		
		enabled = {
			any_owned_location = {
				raw_material = goods:iron
				count >= 20
			}
			produced_in_country:iron >= 50
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = iron_industry
				years = 25
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_establish_factories = {
		icon = production_efficiency_advance
		requires = { mission_develop_iron_industry mission_develop_coal_mining }
		
		enabled = {
			any_owned_location = {
				has_building_with_at_least_one_level = tools_workshop
				count >= 10
			}
			# produced_in_country:manufactured_goods >= 100
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = industrial_factories
				years = 30
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_build_railroads = {
		icon = infrastructure_advance
		requires = { mission_establish_factories }
		
		enabled = {
			has_advance = railroad_advance
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = railroad_network
				years = 35
				mode = add
			}
		}
		ai_will_do = { value = 100 }
	}

	mission_industrial_revolution = {
		icon = empire_icon
		requires = { mission_build_railroads mission_establish_factories }
		
		enabled = {
			total_development >= 3500
			average_country_literacy >= 35
			has_embraced_institution = institution:industrialization
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = industrial_revolution
				years = 40
				mode = add
			}

			trigger_event_silently = england_mission_events.39
		}
		ai_will_do = { value = 100 }
	}

	# ==================== PHASE 12: FINAL ULTIMATE MISSIONS ====================
	
	mission_british_world_hegemony = {
		icon = empire_icon
		requires = { 
			mission_empire_where_sun_never_sets
			mission_industrial_revolution
			mission_glorious_revolution
			mission_rule_the_waves
			
		}
		
		enabled = {
			is_great_power = yes
			is_hegemon = yes
			prestige >= 90
			army_tradition >= 75
			navy_tradition >= 75
			total_development >= 5000
			num_subjects >= 15
			
			# Global trade dominance
			monthly_income_trade_and_tax >= 1000
			
			#@@@ maybe to big need player feedback
			army_size >= 200
			navy_size >= 300

		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = british_world_hegemony
				years = -1
				mode = add
			}

			trigger_event_silently = england_mission_events.40
		}
		ai_will_do = { value = 100 }
	}


	# mission_test_event_one = {
	# 	icon = cultural_acceptance_advance
	# 	requires = {  }
		
	# 	enabled = {
	# 		capital = {
	# 			development >= 10
	# 		}
	# 	}
		
	# 	duration = 0

	# 	on_completion = {
	# 		trigger_event_non_silently = england_mission_events.25
	# 	}
	# 	#ai_will_do = { value = -1000 }
	# }
	# mission_test_event_two = {
	# 	icon = cultural_acceptance_advance
	# 	requires = { mission_test_event_one }
		
	# 	enabled = {
	# 		capital = {
	# 			development >= 10
	# 		}
	# 	}
		
	# 	duration = 0

	# 	on_completion = {
	# 		add_gold = 5000
	# 		# trigger_event_non_silently = england_mission_events.21
	# 	}
	# 	#ai_will_do = { value = -1000 }
	# }
	# mission_test_event_three = {
	# 	icon = cultural_acceptance_advance
	# 	requires = { mission_test_event_two }
		
	# 	enabled = {
	# 		capital = {
	# 			development >= 10
	# 		}
	# 	}
		
	# 	duration = 0

	# 	on_completion = {
	# 		trigger_event_non_silently = england_mission_events.22
	# 		trigger_event_non_silently = england_mission_events.23
	# 		trigger_event_non_silently = england_mission_events.24
	# 		trigger_event_non_silently = england_mission_events.25
	# 		trigger_event_non_silently = england_mission_events.27
	# 		trigger_event_non_silently = england_mission_events.28
	# 	}
	# 	#ai_will_do = { value = -1000 }
	# }

	# mission_test_event_three_half = {
	# 	icon = cultural_acceptance_advance
	# 	requires = { mission_test_event_three }
		
	# 	enabled = {
	# 		capital = {
	# 			development >= 10
	# 		}
	# 	}
		
	# 	duration = 0

	# 	on_completion = {
	# 		trigger_event_non_silently = england_mission_events.31
	# 		trigger_event_non_silently = england_mission_events.32
	# 		trigger_event_non_silently = england_mission_events.33
	# 		trigger_event_non_silently = england_mission_events.34
	# 		trigger_event_non_silently = england_mission_events.35
	# 	}
	# 	#ai_will_do = { value = -1000 }
	# }

	# mission_test_event_four = {
	# 	icon = cultural_acceptance_advance
	# 	requires = { mission_test_event_three_half }
		
	# 	enabled = {
	# 		capital = {
	# 			development >= 10
	# 		}
	# 	}
		
	# 	duration = 0

	# 	on_completion = {
	# 		trigger_event_non_silently = england_mission_events.36
	# 		trigger_event_non_silently = england_mission_events.37
	# 		trigger_event_non_silently = england_mission_events.38
	# 		trigger_event_non_silently = england_mission_events.39
	# 		trigger_event_non_silently = england_mission_events.40
	# 		trigger_event_non_silently = england_mission_events.41
	# 		trigger_event_non_silently = england_mission_events.42
	# 	}
	# 	#ai_will_do = { value = -1000 }
	# }

}