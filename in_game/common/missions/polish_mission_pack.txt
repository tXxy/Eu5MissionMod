polish_mission_pack = {
	icon = generic_centralization
	chance = 3600
	repeatable = no
	player_playstyle = diplomatic

	visible = {
		game_has_missions_enabled = yes
		tag = "POL"
	}

	on_start = {
		set_variable = {
			name = mission_target_development
			value = 25
		}
		set_variable = {
			name = mission_target_manpower
			value = 15000
		}
	}

	on_completion = {
		add_prestige = 50
		add_legitimacy = 25
		remove_variable = mission_target_development
		remove_variable = mission_target_manpower
	}

	on_abort = {
		remove_variable = mission_target_development
		remove_variable = mission_target_manpower
	}

	# === POLISH UNIFICATION & INTERNAL DEVELOPMENT ===
	
	mission_consolidate_polish_heartland = {
		icon = centralization_advance
		requires = { }
		
		enabled = {
			stability >= 5
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = unified_polish_crown
				years = 10
				mode = add
			}
			add_legitimacy = 20
			add_gold = 100
			area:lesser_poland_area = {
				every_location_in_area = {
					change_control = control_mild_bonus
				}
			}
			trigger_event_silently = polish_mission_events.1
		}
	}

	# NEW MISSION: Expand Kraków's Influence
	mission_expand_krakow_influence = {
		icon = cultural_acceptance_advance
		requires = { mission_develop_krakow }
		
		enabled = {
			capital = {
				development >= 45
				total_building_levels >= 35
				has_building_with_at_least_one_level = university
			}
			monthly_income_trade_and_tax >= 75
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = krakow_regional_center
				years = 15
				mode = add
			}
			capital = {
				add_development = 8
				add_location_modifier = {
					modifier = major_cultural_hub
					years = 10
					mode = add
				}
			}
			add_prestige = 25
			trigger_event_silently = polish_mission_events.51
		}
	}

	mission_develop_krakow = {
		icon = cultural_acceptance_advance
		requires = { mission_consolidate_polish_heartland }
		
		enabled = {
			capital = {
				development >= 35
				total_building_levels >= 25
				has_building_with_at_least_one_level = marketplace
			}
			monthly_income >= var:mission_target_development
		}
		
		duration = 0

		on_completion = {
			add_gold = {
				value = monthly_income
				multiply = 8
			}
			capital = {
				add_development = 5
				add_location_modifier = {
					modifier = krakow_cultural_center
					years = 10
					mode = add
				}
			}
			trigger_event_silently = polish_mission_events.2
		}
	}

	mission_strengthen_szlachta = {
		icon = court_accounting
		requires = { mission_consolidate_polish_heartland }
		
		enabled = {
			"estate_power(estate_type:nobles_estate)" >= 0.4
			prestige >= 40
			"estate(estate_type:nobles_estate)" = {
				num_privileges >= 3
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = szlachta_privileges
				years = 10
				mode = add
			}
			add_estate_satisfaction = {
				type = estate_type:nobles_estate
				value = estate_satisfaction_mild_bonus
			}
			trigger_event_silently = polish_mission_events.3
		}
	}

	# === WESTERN EXPANSION & HRE RELATIONS ===
	
	mission_secure_silesia = {
		icon = offensive_army
		requires = { mission_consolidate_polish_heartland }
		
		enabled = {
			area:silesia_area = {
				locations_owned_by_fraction = {
					target = root
					value >= 0.8
				}
			}
			army_tradition >= 30
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = polish_silesian_holdings
				years = 10
				mode = add
			}
			add_manpower = {
				value = max_manpower
				multiply = 0.15
			}
			add_gold = 75
			trigger_event_silently = polish_mission_events.4
		}
	}

	mission_challenge_bohemian_suzerainty = {
		icon = diplomatic_influence
		requires = { mission_secure_silesia }
		
		enabled = {
			army_tradition >= 30
			prestige >= 50
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = central_european_contender
				years = 10
				mode = add
			}
			add_prestige = 30
			add_diplomatic_reputation = 1
			trigger_event_non_silently = polish_mission_events.5
		}
	}

	# NEW MISSION: Establish Baltic Naval Presence
	mission_establish_baltic_navy = {
		icon = maritime_advance_age_2
		requires = { mission_secure_pomerania mission_develop_baltic_ports }
		
		enabled = {
			navy_size >= 25
			num_of_ports >= 5
			any_owned_location = {
				is_coastal = yes
				has_building_with_at_least_one_level = dock
				count >= 4
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = baltic_naval_tradition
				years = 15
				mode = add
			}
			add_navy_tradition = 25
			ordered_owned_location = {
				limit = { is_coastal = yes }
				order_by = development
				max = 3
				add_development = 3
				add_location_modifier = {
					modifier = naval_base
					years = 10
					mode = add
				}
			}
			trigger_event_silently = polish_mission_events.52
		}
	}

	mission_subjugate_teutonic_order = {
		icon = glorious_arms
		requires = { mission_challenge_bohemian_suzerainty }
		
		enabled = {
			OR = {
				NOT = { country_exists = c:TEU }
				c:TEU = {
					is_subject_of = root
				}
			}
			area:east_prussia_area = {
				locations_owned_by_fraction = {
					target = root
					value >= 0.8
				}
			}
			army_tradition >= 50
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = victory_over_teutons
				years = 10
				mode = add
			}
			add_army_tradition = 25
			add_prestige = 30
			set_variable = {
				name = victory_over_teutons_achieved
				value = 1
			}
			if = {
				limit = { country_exists = c:TEU }
				c:TEU = {
					become_subject = {
						type = vassal
						overlord = root
					}
				}
			}
			trigger_event_silently = polish_mission_events.6
		}
	}

	mission_secure_pomerania = {
		icon = maritime_advance_age_2
		requires = { mission_subjugate_teutonic_order }
		
		enabled = {
			area:pomerelia_area = {
				locations_owned_by_fraction = {
					target = root
					value >= 0.9
				}
			}
			num_of_ports >= 3
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = baltic_access
				years = 10
				mode = add
			}
			add_navy_tradition = 15
			ordered_owned_location = {
				limit = { is_coastal = yes }
				order_by = development
				max = 2
				add_location_modifier = {
					modifier = polish_baltic_port
					years = 5
					mode = add
				}
			}
			trigger_event_silently = polish_mission_events.7
		}
	}

	mission_rival_hre_emperor = {
		icon = bill_of_rights
		requires = { mission_challenge_bohemian_suzerainty }
		
		enabled = {
			prestige >= 70
			army_tradition >= 40
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = hre_counterweight
				years = 15
				mode = add
			}
			add_prestige = 50
			trigger_event_non_silently = polish_mission_events.8
		}
	}

	# === EASTERN EXPANSION & RUTHENIAN INTEGRATION ===
	
	mission_secure_galicia_volhynia = {
		icon = defensive_army
		requires = { mission_consolidate_polish_heartland }
		
		enabled = {
			area:volhynia_area = {
				locations_owned_by_fraction = {
					target = root
					value >= 0.7
				}
			}
			army_tradition >= 25
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = polish_eastern_marches
				years = 10
				mode = add
			}
			add_manpower = {
				value = max_manpower
				multiply = 0.20
			}
			trigger_event_silently = polish_mission_events.9
		}
	}

	mission_expand_into_red_ruthenia = {
		icon = cultural_acceptance_advance
		requires = { mission_secure_galicia_volhynia }
		
		enabled = {
			area:red_ruthenia_area = {
				locations_owned_by_fraction = {
					target = root
					value >= 0.75
				}
			}
			accepted_cultures >= 2
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = multicultural_expansion
				years = 10
				mode = add
			}
			add_gold = 100
			area:red_ruthenia_area = {
				every_location_in_area = {
					change_control = control_weak_bonus
					change_prosperity = prosperity_very_weak_bonus
				}
			}
			trigger_event_non_silently = polish_mission_events.10
		}
	}

	mission_integrate_ruthenian_culture = {
		icon = cultural_acceptance_advance
		requires = { mission_expand_into_red_ruthenia }
		
		enabled = {
			culture:ruthenian = {
				accepted = yes
			}
			any_owned_location = {
				culture = culture:ruthenian
				has_building_with_at_least_one_level = orthodox_church
				count >= 3
			}
			estate_satisfaction:clergy_estate >= 0.6
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = ruthenian_integration
				years = 15
				mode = add
			}
			add_estate_satisfaction = {
				type = estate_type:clergy_estate
				value = estate_satisfaction_mild_bonus
			}
			change_societal_value = {
				type = spiritualist_vs_humanist
				value = societal_value_move_to_left
			}
			trigger_event_silently = polish_mission_events.11
		}
	}

	# NEW MISSION: Secure the Cossack Frontier
	mission_secure_cossack_frontier = {
		icon = defensive_army
		requires = { mission_integrate_ruthenian_culture }
		
		enabled = {
			any_owned_location = {
				plc_cossack_interest_location_trigger = yes
				count >= 15
			}
			estate_satisfaction:cossacks_estate >= 0.6
			army_tradition >= 40
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = cossack_frontier_defense
				years = 15
				mode = add
			}
			add_estate_satisfaction = {
				type = estate_type:cossacks_estate
				value = estate_satisfaction_mild_bonus
			}
			add_manpower = {
				value = max_manpower
				multiply = 0.25
			}
			trigger_event_silently = polish_mission_events.53
		}
	}

	# === LITHUANIAN DIPLOMACY & COMMONWEALTH ===
	
	mission_forge_lithuanian_alliance = {
		icon = diplomatic_influence
		requires = { mission_expand_krakow_influence }
		
		enabled = {
			country_exists = c:LIT
			c:LIT = {
				is_allied_with = { target = root }
				trust = { target = root value >= 70 }
			}
			prestige >= 45
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = polish_lithuanian_cooperation
				years = 10
				mode = add
			}
			add_diplomatic_reputation = 1
			add_gold = 75
			trigger_event_silently = polish_mission_events.12
		}
	}

	mission_form_polish_lithuanian_commonwealth = {
		icon = empire_icon
		requires = { mission_forge_lithuanian_alliance }
		
		enabled = {
			OR = {
				is_personal_unity_overlord_with = c:LIT
				AND = {
					country_exists = c:LIT
					c:LIT = {
						is_subject_of = root
					}
				}
			}
			owns = location:krakow
			owns = location:vilnius
			owns = location:warsaw
			prestige >= 75
			legitimacy >= 70
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = polish_lithuanian_commonwealth
				years = -1
				mode = add
			}
			add_prestige = 60
			add_stability = 30
			trigger_event_non_silently = polish_mission_events.14
		}
	}

	# === ECONOMIC DEVELOPMENT & TRADE ===
	
	mission_develop_agricultural_heartland = {
		icon = expanded_supply_trains
		requires = { mission_consolidate_polish_heartland }
		
		enabled = {
			any_owned_location = {
				count >= 8
				has_building_with_at_least_one_level = farm_estate
			}
			monthly_income_production > 15
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = polish_agricultural_wealth
				years = 10
				mode = add
			}
			add_gold = {
				value = monthly_income
				multiply = 6
			}
			trigger_event_silently = polish_mission_events.15
		}
	}

	mission_establish_grain_trade = {
		icon = merchants_and_trade
		requires = { mission_develop_agricultural_heartland }
		
		enabled = {
			trade_share_in_grain >= 0.15
			any_owned_location = {
				has_building_with_at_least_one_level = marketplace
				count >= 5
			}
			monthly_income_trade_and_tax > 50
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = grain_trade_dominance
				years = 10
				mode = add
			}
			add_estate_satisfaction = {
				type = estate_type:burghers_estate
				value = estate_satisfaction_mild_bonus
			}
			trigger_event_silently = polish_mission_events.16
		}
	}

	mission_develop_baltic_ports = {
		icon = ship_building_advance
		requires = { mission_establish_grain_trade mission_subjugate_teutonic_order }
		
		enabled = {
			any_owned_location = {
				is_coastal = yes
				has_building_with_at_least_one_level = dock
				count >= 3
			}
			navy_size >= 15
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = baltic_merchant_marine
				years = 10
				mode = add
			}
			add_navy_tradition = 20
			ordered_owned_location = {
				limit = { is_coastal = yes }
				order_by = development
				max = 3
				add_development = 3
			}
			trigger_event_silently = polish_mission_events.17
		}
	}

	# NEW MISSION: Develop the Vistula Trade Network
	mission_develop_vistula_trade = {
		icon = merchants_and_trade
		requires = { mission_establish_grain_trade }
		
		enabled = {
			any_owned_location = {
				is_on_river = yes
				has_building_with_at_least_one_level = marketplace
				count >= 8
			}
			trade_power_in_inland_nodes >= 100
			monthly_income_trade_and_tax > 75
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = vistula_trade_network
				years = 15
				mode = add
			}
			add_trade_power = 25
			add_gold = {
				value = monthly_income_trade_and_tax
				multiply = 8
			}
			trigger_event_silently = polish_mission_events.54
		}
	}

	# === MILITARY REFORMS & EXPANSION ===
	
	mission_establish_winged_hussars = {
		icon = cavalry_advance
		requires = { mission_reform_polish_army }
		
		enabled = {
			military_strength >= 60
			army_tradition >= 60
			technology_group = technology_group:eastern
			any_owned_location = {
				has_building_with_at_least_one_level = stables
				count >= 3
			}
			estate_satisfaction:nobles_estate >= 0.65
			has_variable = victory_over_teutons_achieved
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = winged_hussars_elite
				years = -1
				mode = add
			}
			add_army_tradition = 30
			trigger_event_silently = polish_mission_events.20
		}
	}

	mission_reform_polish_army = {
		icon = medieval_military
		requires = { mission_promote_polish_culture }
		
		enabled = {
			army_professionalism >= 0.4
			military_tech_level >= 12
			total_regiments >= 30
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = reformed_polish_army
				years = 10
				mode = add
			}
			add_manpower = {
				value = max_manpower
				multiply = 0.25
			}
			trigger_event_silently = polish_mission_events.21
		}
	}

	mission_fortify_eastern_border = {
		icon = defensive_army
		requires = { mission_expand_into_red_ruthenia }
		
		enabled = {
			any_owned_location = {
				has_building_with_at_least_one_level = castle
				count >= 8
			}
			fort_maintenance_cost <= 0.8
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = eastern_fortification_line
				years = 15
				mode = add
			}
			add_army_tradition = 15
			trigger_event_silently = polish_mission_events.22
		}
	}

	mission_develop_artillery_corps = {
		icon = glorious_arms
		requires = { mission_reform_polish_army }
		
		enabled = {
			military_tech_level >= 16
			army_professionalism >= 0.6
			any_owned_location = {
				has_building_with_at_least_one_level = barracks
				count >= 5
			}
			total_regiments >= 50
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = polish_artillery_corps
				years = 15
				mode = add
			}
			add_army_tradition = 25
			add_manpower = {
				value = max_manpower
				multiply = 0.20
			}
			trigger_event_silently = polish_mission_events.23
		}
	}

	# === CULTURAL & RELIGIOUS DEVELOPMENT ===
	
	mission_promote_polish_culture = {
		icon = cultural_acceptance_advance
		requires = { mission_consolidate_polish_heartland }
		
		enabled = {
			dominant_culture = culture:polish
			cultural_unity >= 0.7
			prestige >= 40
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = polish_cultural_hegemony
				years = 10
				mode = add
			}
			culture = { add_cultural_tradition = cultural_tradition_mild_bonus }
			trigger_event_silently = polish_mission_events.24
		}
	}

	mission_establish_jagiellonian_university = {
		icon = bill_of_rights
		requires = { mission_promote_polish_culture }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = university
			}
			average_development >= 12
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = jagiellonian_university
				years = -1
				mode = add
			}
			add_technology_progress = {
				value = 200
				type = all
			}
			trigger_event_silently = polish_mission_events.25
		}
	}

	mission_defend_christendom = {
		icon = glorious_arms
		requires = { mission_promote_polish_culture }
		
		enabled = {
			religion = religion:catholic
			any_neighbor_country = {
				religion.group = religion_group:muslim
				military_strength >= 40
			}
			army_tradition >= 50
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = bulwark_of_christendom
				years = 10
				mode = add
			}
			add_prestige = 40
			add_church_power = 50
			trigger_event_silently = polish_mission_events.26
		}
	}

	mission_promote_counter_reformation = {
		icon = improved_relation_impact_renaissance
		requires = { mission_defend_christendom }
		
		enabled = {
			religion = religion:catholic
			any_neighbor_country = {
				religion.group = religion_group:protestant
				exists = yes
			}
			church_power >= 100
			prestige >= 70
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = counter_reformation_champion
				years = 20
				mode = add
			}
			add_church_power = 100
			add_estate_satisfaction = {
				type = estate_type:clergy_estate
				value = estate_satisfaction_severe_bonus
			}
			trigger_event_non_silently = polish_mission_events.35
		}
	}

	# NEW MISSION: Promote Sarmatism
	mission_promote_sarmatism = {
		icon = cultural_acceptance_advance
		requires = { mission_form_polish_lithuanian_commonwealth }
		
		enabled = {
			societal_value:aristocracy_vs_plutocracy <= -60
			prestige >= 60
			"estate_power(estate_type:nobles_estate)" >= 0.35
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = sarmatian_culture
				years = 20
				mode = add
			}
			add_estate_satisfaction = {
				type = estate_type:nobles_estate
				value = estate_satisfaction_mild_bonus
			}
			add_prestige = 30
			trigger_event_silently = polish_mission_events.55
		}
	}

	# === ESTATE MANAGEMENT & GOLDEN LIBERTY ===
	
	mission_balance_szlachta_power = {
		icon = government_reforms
		requires = { mission_strengthen_szlachta }
		
		enabled = {
			"estate_power(estate_type:nobles_estate)" >= 0.3
			"estate_power(estate_type:nobles_estate)" <= 0.6
			estate_satisfaction:nobles_estate >= 0.70
			estate_satisfaction:burghers_estate >= 0.60
			estate_satisfaction:peasants_estate >= 0.50
		}
		
		duration = 730
		modifier_while_progressing = {
			stability_investment = 2
			global_monthly_control = 0.01
		}

		on_monthly = {
			random_list = {
				15 = { trigger_event_silently = polish_mission_events.40 } # Szlachta Demands
				10 = { trigger_event_silently = polish_mission_events.41 } # Burgher Complaints
				10 = { trigger_event_silently = polish_mission_events.42 } # Peasant Unrest
				65 = { }
			}
		}

		on_completion = {
			add_country_modifier = {
				modifier = golden_liberty_balance
				years = 15
				mode = add
			}
			add_stability = 25
			add_legitimacy = 25
			trigger_event_silently = polish_mission_events.43
		}
	}

	mission_implement_liberum_veto = {
		icon = bill_of_rights
		requires = { mission_balance_szlachta_power }
		
		enabled = {
			"estate_power(estate_type:nobles_estate)" >= 0.4
			government_type = government_type:monarchy
			estate_satisfaction:nobles_estate >= 0.75
			prestige >= 80
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = liberum_veto
				years = -1
				mode = add
			}
			add_estate_satisfaction = {
				type = estate_type:nobles_estate
				value = estate_satisfaction_severe_bonus
			}
			add_stability = -25
			trigger_event_non_silently = polish_mission_events.45
		}
	}

	# NEW MISSION: Consolidate Golden Liberty
	mission_consolidate_golden_liberty = {
		icon = government_reforms
		requires = { mission_implement_liberum_veto }
		
		enabled = {
			calc_true_if = {
				has_estate_privilege = estate_privilege:golden_liberty
				has_estate_privilege = estate_privilege:liberum_veto
				has_estate_privilege = estate_privilege:pacta_conventa
				amount >= 3
			}
			estate_satisfaction:nobles_estate >= 0.85
			prestige >= 90
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = golden_liberty_established
				years = -1
				mode = add
			}
			add_prestige = 50
			add_legitimacy = 40
			trigger_event_non_silently = polish_mission_events.56
		}
	}

	mission_develop_rural_economy = {
		icon = expanded_supply_trains
		requires = { mission_develop_agricultural_heartland }
		
		enabled = {
			any_owned_rural_location = {
				has_building_with_at_least_one_level = farm_estate
				count >= 15
			}
			any_owned_rural_location = {
				has_building_with_at_least_one_level = market_village
				count >= 10
			}
			monthly_income_production > 30
			estate_satisfaction:peasants_estate >= 0.65
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = rural_development_boom
				years = 15
				mode = add
			}
			add_gold = {
				value = monthly_income
				multiply = 15
			}
			every_owned_rural_location = {
				limit = { has_building_with_at_least_one_level = farm_estate }
				change_prosperity = prosperity_weak_bonus
			}
			trigger_event_silently = polish_mission_events.46
		}
	}

	# === MAJOR ACHIEVEMENTS & GOLDEN AGE ===
	
	mission_dominate_eastern_europe = {
		icon = empire_icon
		requires = { 
			mission_secure_pomerania
			
			
		}
		
		enabled = {
			total_development >= 500
			military_strength >= 80
			prestige >= 90
			num_of_subjects >= 3
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = eastern_european_hegemony
				years = 15
				mode = add
			}
			add_prestige = 75
			add_legitimacy = 50
			trigger_event_non_silently = polish_mission_events.30
		}
	}

	mission_polish_golden_age = {
		icon = bill_of_rights
		requires = { 
			mission_dominate_eastern_europe
			mission_consolidate_golden_liberty
		}
		final = yes
		
		enabled = {
			prestige >= 95
			stability >= 80
			monthly_income_trade_and_tax > 200
			golden_era = yes
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = polish_golden_age
				years = -1
				mode = add
			}
			add_gold = {
				value = monthly_income
				multiply = 24
			}
			add_prestige = 100
			trigger_event_non_silently = polish_mission_events.50
		}
	}
}