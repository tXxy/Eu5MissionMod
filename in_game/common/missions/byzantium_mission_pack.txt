byzantium_mission_pack = {
	icon = empire_icon
	chance = 3600
	repeatable = no
	player_playstyle = military

	visible = {
		game_has_missions_enabled = yes
		tag = "BYZ"
	}

	on_start = {
		set_variable = {
			name = mission_target_navy_size
			value = 25
		}
		set_variable = {
			name = mission_target_silk_production
			value = 12
		}
	}

	on_completion = {
		add_prestige = 100
		add_legitimacy = 50
		remove_variable = mission_target_navy_size
		remove_variable = mission_target_silk_production
	}

	on_abort = {
		remove_variable = mission_target_navy_size
		remove_variable = mission_target_silk_production
	}

	# === BYZANTINE SURVIVAL & CONSOLIDATION ===
	
	mission_secure_constantinople = {
		icon = defensive_army
		requires = { }
		
		enabled = {
			capital = {
				control >= 90
				garrison >= 5000
			}
			manpower >= 10000
			stability >= 40
			legitimacy >= 50
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = theodosian_walls_restored
					years = 25
				}
				change_defensiveness = 0.3
			}
			add_prestige = 25
			trigger_event_silently = byz_mission_events.1
		}
	}

	mission_consolidate_thrace = {
		icon = defensive_army
		requires = { mission_secure_constantinople }
		
		enabled = {
			area:thrace_area ?= {
				any_location_in_area = {
					percent >= 1
					OR = {
						owner ?= root
						owner ?= { is_subject_of = root }
					}
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_manpower = {
				value = max_manpower
				multiply = 0.15
			}
			add_country_modifier = {
				modifier = secure_hinterland
				years = 10
			}
		}
	}

	mission_curtail_nobles_power = {
		icon = government_reforms
		requires = { mission_secure_constantinople }
		
		enabled = {
			"estate_power(estate_type:nobles_estate)" <= 0.40
			"estate_power(estate_type:crown_estate)" >= 0.35
			legitimacy >= 65
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = imperial_authority_restored
				years = 15
			}
			add_stability = 20
		}
	}

	mission_restore_greek_fire = {
		icon = artillery_icon
		requires = { mission_secure_constantinople }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = cannon_maker
			}
			any_owned_location = {
				raw_material = goods:chemicals
				count >= 3
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = greek_fire_arsenal
				years = -1
			}
			add_navy_tradition = 25
		}
	}

	# === NAVAL REVIVAL & TRADE ===
	
	mission_repair_golden_horn = {
		icon = ship_building_advance
		requires = { mission_secure_constantinople }
		
		enabled = {
			num_of_ports >= 3
			capital = {
				has_building_with_at_least_one_level = dock
			}
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = golden_horn_shipyards
					years = 20
				}
			}
			add_navy_tradition = 15
		}
	}

	mission_rebuild_byzantine_navy = {
		icon = maritime_advance_age_2
		requires = { mission_repair_golden_horn }
		
		enabled = {
			navy_size >= var:mission_target_navy_size
			num_of_ports >= 6
			any_owned_location = {
				has_building_with_at_least_one_level = wharf
				count >= 4
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = restored_byzantine_navy
				years = 15
			}
			add_navy_tradition = 20
		}
	}

	mission_dominate_bosporus = {
		icon = maritime_advance_age_2
		requires = { mission_rebuild_byzantine_navy mission_consolidate_thrace }
		
		enabled = {
			location:gallipoli = {
				owner ?= root
			}
			location:izmit = {
				owner ?= root
			}
			navy_tradition >= 50
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = bosporus_control
				years = -1
			}
			add_gold = 250
		}
	}

	mission_secure_aegean = {
		icon = ship_building_advance
		requires = { mission_rebuild_byzantine_navy }
		
		enabled = {
			area:aegean_islands_area ?= {
				any_location_in_area = {
					percent >= 0.8
					OR = {
						owner ?= root
						owner ?= { is_subject_of = root }
					}
				}
			}
			navy_size >= 35
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = aegean_dominance
				years = 15
			}
			add_trade_efficiency = 0.1
		}
	}

	# === RECONQUEST OF GREECE ===
	
	mission_recover_thessaloniki = {
		icon = offensive_army
		requires = { mission_consolidate_thrace }
		
		enabled = {
			location:thessaloniki = {
				owner ?= root
				control >= 80
			}
			area:macedonia_area ?= {
				any_location_in_area = {
					percent >= 0.6
					OR = {
						owner ?= root
						owner ?= { is_subject_of = root }
					}
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = second_city_restored
				years = 10
			}
			location:thessaloniki = {
				change_development = 3
			}
		}
	}

	mission_secure_athens = {
		icon = cultural_acceptance_advance
		requires = { mission_recover_thessaloniki }
		
		enabled = {
			location:athens = {
				owner ?= root
				has_building_with_at_least_one_level = library
			}
			area:central_greece_area ?= {
				any_location_in_area = {
					percent >= 0.7
					OR = {
						owner ?= root
						owner ?= { is_subject_of = root }
					}
				}
			}
		}
		
		duration = 0

		on_completion = {
			location:athens = {
				add_location_modifier = {
					modifier = athenian_academy
					years = 15
				}
			}
			add_research_progress = research_progress_mild_bonus
		}
	}

	mission_reconstruct_hexamilion = {
		icon = defensive_army
		requires = { mission_secure_athens }
		
		enabled = {
			location:corinth = {
				owner ?= root
				has_building_with_at_least_one_level = castle
				fort_level >= 3
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = hexamilion_wall
				years = -1
			}
			add_defensiveness = 0.15
			trigger_event_silently = byz_mission_events.2
		}
	}

	mission_unite_morea = {
		icon = defensive_army
		requires = { mission_reconstruct_hexamilion }
		
		enabled = {
			area:morea_area ?= {
				any_location_in_area = {
					percent >= 0.9
					OR = {
						owner ?= root
						owner ?= { is_subject_of = root }
					}
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = morea_secure
				years = 10
			}
			add_manpower = {
				value = max_manpower
				multiply = 0.1
			}
		}
	}

	# === ANATOLIAN RECONQUEST ===
	
	mission_cross_into_anatolia = {
		icon = offensive_army
		requires = { mission_dominate_bosporus }
		
		enabled = {
			region:anatolia_region ?= {
				any_location_in_region = {
					owner ?= root
					count >= 5
				}
			}
			army_tradition >= 40
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = anatolian_bridgehead
				years = 8
			}
			add_prestige = 20
		}
	}

	mission_liberate_nicaea = {
		icon = religious_icon
		requires = { mission_cross_into_anatolia }
		
		enabled = {
			location:iznik = {
				owner ?= root
				religion = religion:orthodox
			}
			location:izmit = {
				owner ?= root
			}
		}
		
		duration = 0

		on_completion = {
			add_legitimacy = 25
			add_stability = 20
			trigger_event_silently = byz_mission_events.3
		}
	}

	mission_recover_bithynia = {
		icon = offensive_army
		requires = { mission_liberate_nicaea }
		
		enabled = {
			area:bithynia_area ?= {
				any_location_in_area = {
					percent >= 0.9
					OR = {
						owner ?= root
						owner ?= { is_subject_of = root }
					}
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = bithynia_secure
				years = 10
			}
			add_manpower = {
				value = max_manpower
				multiply = 0.15
			}
		}
	}

	mission_secure_aegean_coast = {
		icon = maritime_advance_age_2
		requires = { mission_liberate_nicaea mission_secure_aegean }
		
		enabled = {
			location:smyrna = {
				owner ?= root
			}
			location:ephesus = {
				owner ?= root
			}
		}
		
		duration = 0

		on_completion = {
			add_trade_efficiency = 0.08
			add_country_modifier = {
				modifier = ionian_revival
				years = 10
			}
		}
	}

	mission_break_ottoman_power = {
		icon = war_exhaustion
		requires = { mission_liberate_nicaea }
		
		enabled = {
			OR = {
				NOT = { country_exists = c:TUR }
				c:TUR = { is_subject_of = root }
				AND = {
					c:TUR = {
						relative_strength = {
							target = root
							value <= 0.6
						}
					}
				}
			}
			owns = location:bursa
		}
		
		duration = 0

		on_completion = {
			add_prestige = 50
			add_power_projection = 30
			add_country_modifier = {
				modifier = turkish_threat_ended
				years = 25
			}
			trigger_event_silently = byz_mission_events.4
		}
	}

	mission_restore_anatolian_themes = {
		icon = administrative_efficiency
		requires = { mission_break_ottoman_power mission_recover_bithynia mission_secure_aegean_coast }
		
		enabled = {
			region:anatolia_region ?= {
				locations_owned_by_fraction ?= {
					target = root
					value >= 0.5
				}
			}
			average_control_in_home_region >= 0.7
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = theme_system_restored
				years = -1
			}
			add_administrative_efficiency = 0.05
		}
	}

	# === BALKAN RECONQUEST ===
	
	mission_subjugate_bulgaria = {
		icon = glorious_arms
		requires = { mission_consolidate_thrace }
		
		enabled = {
			OR = {
				NOT = { country_exists = c:BUL }
				c:BUL = { is_subject_of = root }
			}
			location:tarnovo = {
				owner ?= root
			}
		}
		
		duration = 0

		on_completion = {
			add_prestige = 35
			add_country_modifier = {
				modifier = bulgarian_subjugation
				years = 15
			}
		}
	}

	mission_secure_serbia = {
		icon = diplomatic_influence
		requires = { mission_subjugate_bulgaria }
		
		enabled = {
			OR = {
				NOT = { country_exists = c:SER }
				c:SER = { is_subject_of = root }
			}
			area:serbia_area ?= {
				any_location_in_area = {
					percent >= 0.6
					OR = {
						owner ?= root
						owner ?= { is_subject_of = root }
					}
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_gold = 300
			add_country_modifier = {
				modifier = serbian_vassalage
				years = 12
			}
		}
	}

	mission_secure_bosnia = {
		icon = offensive_army
		requires = { mission_secure_serbia }
		
		enabled = {
			area:bosnia_area ?= {
				any_location_in_area = {
					percent >= 0.7
					OR = {
						owner ?= root
						owner ?= { is_subject_of = root }
					}
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_manpower = {
				value = max_manpower
				multiply = 0.1
			}
			add_prestige = 20
		}
	}

	# === RELIGIOUS & CULTURAL REVIVAL ===
	
	mission_restore_hagia_sophia = {
		icon = religious_icon
		requires = { mission_secure_constantinople }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = cathedral
				development >= 40
			}
			religious_unity >= 0.8
		}
		
		duration = 0

		on_completion = {
			add_legitimacy = 30
			add_prestige = 40
			capital = {
				add_location_modifier = {
					modifier = hagia_sophia_restored
					years = -1
				}
			}
		}
	}

	mission_protect_mount_athos = {
		icon = religious_icon
		requires = { mission_restore_hagia_sophia }
		
		enabled = {
			location:halkidiki = {
				owner ?= root
				religion = religion:orthodox
				has_building_with_at_least_one_level = monastery
			}
		}
		
		duration = 0

		on_completion = {
			location:halkidiki = {
				add_location_modifier = {
					modifier = mount_athos_autonomous
					years = -1
				}
			}
			add_prestige = 25
		}
	}

	mission_revive_constantinople_university = {
		icon = technology_icon
		requires = { mission_restore_hagia_sophia }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = university
				development >= 45
			}
			has_advance = greek_rite
			average_country_literacy >= 30
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = constantinople_university
				years = -1
			}
			add_research_progress = research_progress_severe_bonus
		}
	}

	mission_restore_orthodox_patriarchates = {
		icon = religious_icon
		requires = { mission_protect_mount_athos }
		
		enabled = {
			capital.market ?= {
				"merchant_power_in_market(root)" >= 60
			}
			any_country = {
				religion = religion:orthodox
				is_allied_with = root
				count >= 3
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = orthodox_leadership
				years = 20
			}
			add_diplomatic_reputation = 1
		}
	}

	# === TRADE & ECONOMIC DEVELOPMENT ===
	
	mission_develop_silk_trade = {
		icon = merchants_and_trade
		requires = { mission_curtail_nobles_power }
		
		enabled = {
			any_owned_location = {
				raw_material = goods:silk
				has_building_with_at_least_one_level = textile_manufactory
				count >= 4
			}
			produced_in_country:silk >= var:mission_target_silk_production
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = byzantine_silk_trade
				years = 20
			}
			add_gold = {
				value = monthly_income
				multiply = 10
			}
		}
	}

	mission_control_black_sea_trade = {
		icon = trade_efficiency
		requires = { mission_develop_silk_trade mission_dominate_bosporus }
		
		enabled = {
			location:trebizond = {
				owner ?= root
			}
			location:crimea = {
				owner ?= root
			}
			capital.market ?= {
				"merchant_power_in_market(root)" >= 70
			}
		}
		
		duration = 0

		on_completion = {
			add_trade_efficiency = 0.12
			add_country_modifier = {
				modifier = black_sea_dominance
				years = 15
			}
		}
	}

	# === GRAND RECONQUEST & EMPIRE BUILDING ===
	
	mission_recover_antioch = {
		icon = religious_icon
		requires = { mission_restore_anatolian_themes }
		
		enabled = {
			location:antioch = {
				owner ?= root
				religion = religion:orthodox
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = antioch_liberated
				years = 15
			}
			add_prestige = 40
			trigger_event_silently = byz_mission_events.5
		}
	}

	mission_recover_alexandria = {
		icon = cultural_acceptance_advance
		requires = { mission_recover_antioch }
		
		enabled = {
			location:alexandria = {
				owner ?= root
				religion = religion:orthodox
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = alexandria_restored
				years = 15
			}
			add_gold = 500
			trigger_event_silently = byz_mission_events.6
		}
	}

	mission_recover_jerusalem = {
		icon = religious_icon
		requires = { mission_recover_alexandria }
		
		enabled = {
			location:jerusalem = {
				owner ?= root
				religion = religion:orthodox
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = jerusalem_liberated
				years = -1
			}
			add_prestige = 75
			add_legitimacy = 50
			trigger_event_silently = byz_mission_events.7
		}
	}

	mission_restore_pentarchy = {
		icon = empire_icon
		requires = { mission_recover_jerusalem mission_restore_orthodox_patriarchates }
		
		enabled = {
			owns = location:constantinople
			owns = location:antioch
			owns = location:alexandria
			owns = location:jerusalem
			owns = location:rome
			
			location:rome = {
				religion = religion:orthodox
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = pentarchy_restored
				years = -1
			}
			add_prestige = 100
			trigger_event_silently = byz_mission_events.8
		}
	}

	mission_restore_roman_empire = {
		icon = empire_icon
		requires = { 
			mission_restore_pentarchy
			mission_secure_bosnia
			mission_restore_anatolian_themes
		}
		final = yes
		
		enabled = {
			is_great_power = yes
			prestige >= 90
			army_tradition >= 70
			
			# Control key Roman territories
			region:italy_region ?= {
				locations_owned_by_fraction ?= {
					target = root
					value >= 0.6
				}
			}
			region:balkans_region ?= {
				locations_owned_by_fraction ?= {
					target = root
					value >= 0.8
				}
			}
			region:anatolia_region ?= {
				locations_owned_by_fraction ?= {
					target = root
					value >= 0.7
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = roman_empire_restored
				years = -1
			}
			add_prestige = 150
			add_legitimacy = 100
			trigger_event_silently = byz_mission_events.9
		}
	}
}