venice_mission_pack = {
	icon = generic_capital_economy
	chance = 3600
	repeatable = no
	player_playstyle = diplomatic

	#ai_will_do = { value = 100 }

	visible = {
		game_has_missions_enabled = yes
		tag = "VEN"
	}

	on_start = {
		set_variable = {
			name = mission_target_trade_income
			value = 200
		}
		set_variable = {
			name = mission_target_galley_count
			value = 40
		}
		set_variable = {
			name = mission_target_merchant_power
			value = 85
		}
	}

	on_completion = {
		add_prestige = 75
		add_republican_tradition = 10
		remove_variable = mission_target_trade_income
		remove_variable = mission_target_galley_count
		remove_variable = mission_target_merchant_power
	}

	on_abort = {
		remove_variable = mission_target_trade_income
		remove_variable = mission_target_galley_count
		remove_variable = mission_target_merchant_power
	}

	# ==================== PHASE 1: VENETIAN LAGOON CONSOLIDATION ====================
	
	mission_secure_venetian_lagoon = {
		icon = defensive_army
		requires = { }
		
		enabled = {
			stability >= 30
			republican_tradition >= 60
			owns = location:venezia
			owns = location:murano
			owns = location:chioggia
		}
		
		duration = 0

		on_completion = {
			add_republican_tradition = 10
			trigger_event_silently = venice_mission_events.1
		}
		#ai_will_do = { value = 100 }
	}

	mission_develop_arsenal = {
		icon = ship_building_advance
		requires = { mission_secure_venetian_lagoon }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = dock
				development >= 40
			}
			total_effective_building_levels:dock >= 8
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = venetian_arsenal_expanded
					years = 35
					mode = add
				}
				change_building_level_in_location = {
					building = building_type:shipyard
					value = 1
					owner ?= root
				}
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_strengthen_republic = {
		icon = government_reforms
		requires = { mission_secure_venetian_lagoon }
		
		enabled = {
			"estate_power(estate_type:burghers_estate)" >= 0.35
			"estate_power(estate_type:nobles_estate)" <= 0.25
			republican_tradition >= 70
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = venetian_constitution
				years = -1
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_develop_murano_glass = {
		icon = production_efficiency_advance
		requires = { mission_secure_venetian_lagoon }
		
		enabled = {
			location:murano = {
				owner ?= root
				has_building_with_at_least_one_level = glass_workshop
				development >= 20
			}
		}
		
		duration = 0

		on_completion = {
			location:murano = {
				add_location_modifier = {
					modifier = murano_glass_monopoly
					years = -1
					mode = add
				}
				change_development = 2
			}
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 2: ADRIATIC DOMINATION ====================
	
	mission_control_adriatic = {
		icon = maritime_advance_age_2
		requires = { mission_develop_arsenal }
		
		enabled = {
			navy_size >= var:mission_target_galley_count
			sea_zone:adriatic_sea = {
				naval_supremacy >= 0.8
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = adriatic_guardian
				years = 35
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_secure_istria_dalmatia = {
		icon = offensive_army
		requires = { mission_control_adriatic }
		
		enabled = {
			area:istria_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
							text = is_owner_generic_check
							owner ?= root
						}
						custom_tooltip = {
							text = is_owner_generic_subject_check
							owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.8
				}
			}
			area:dalmacija_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
							text = is_owner_generic_check
							owner ?= root
						}
						custom_tooltip = {
							text = is_owner_generic_subject_check
							owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.7
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = dalmatian_coast_secure
				years = 25
				mode = add
			}
			area:dalmacija_area = {
				ordered_location_in_area = {
					limit = { owner ?= root }
					order_by = development
					max = 5
					add_core = root
				}
			}
			trigger_event_silently = venice_mission_events.2
		}
		#ai_will_do = { value = 100 }
	}

	mission_subjugate_ragusa = {
		icon = diplomatic_influence
		requires = { mission_secure_istria_dalmatia }
		
		enabled = {
			OR = {
				NOT = { country_exists = c:RAG }
				c:RAG = {
					is_subject_of = root
				}
			}
			location:dubrovnik = {
				owner ?= root
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = ragusan_tributary
				years = 20
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_secure_albania = {
		icon = offensive_army
		requires = { mission_control_adriatic }
		
		enabled = {
			area:albania_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
							text = is_owner_generic_check
							owner ?= root
						}
						custom_tooltip = {
							text = is_owner_generic_subject_check
							owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.7
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_manpower = {
				value = max_manpower
				multiply = 0.2
			}
			area:albania_area = {
				ordered_location_in_area = {
					limit = { owner ?= root }
					order_by = development
					max = 3
					add_core = root
				}
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_control_corfu = {
		icon = maritime_advance_age_2
		requires = { mission_secure_albania }
		
		enabled = {
			location:kerkyra = {
				owner ?= root
				has_building_with_at_least_one_level = castle
			}
		}
		
		duration = 0

		on_completion = {
			location:kerkyra = {
				add_location_modifier = {
					modifier = adriatic_sentinel
					years = 35
					mode = add
				}
				add_core = root
			}
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 3: MEDITERRANEAN TRADE EMPIRE ====================
	
	mission_dominate_levantine_trade = {
		icon = merchants_and_trade
		requires = { mission_control_adriatic }
		
		enabled = {
			monthly_income_trade_and_tax >= 150
			capital.market = {
				"merchant_power_in_market(root)" >= 75
			}
			any_subject = {
				is_subject_type = trade_city
				count >= 1
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = levant_trade_primacy
				years = 30
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_establish_levant_quarters = {
		icon = merchants_and_trade
		requires = { mission_dominate_levantine_trade }
		
		enabled = {
			region:levant_region = {
				any_location_in_region = {
					owner ?= root
					has_building_with_at_least_one_level = merchants_quarters
					count >= 3
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = levant_trading_posts
				years = 25
				mode = add
			}
			add_estate_satisfaction = {
				type = estate_type:burghers_estate
				value = estate_satisfaction_mild_bonus
			}
			trigger_event_silently = venice_mission_events.3
		}
		#ai_will_do = { value = 100 }
	}

	mission_control_cyprus = {
		icon = glorious_arms
		requires = { mission_establish_levant_quarters }
		
		enabled = {
			region:cyprus_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
							text = is_owner_generic_check
							owner ?= root
						}
						custom_tooltip = {
							text = is_owner_generic_subject_check
							owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.9
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = cyprus_crown
				years = 25
				mode = add
			}
			add_prestige = 15
			trigger_event_silently = venice_mission_events.4
		}
		#ai_will_do = { value = 100 }
	}

	mission_secure_crete = {
		icon = defensive_army
		requires = { mission_control_adriatic }
		
		enabled = {
			region:crete_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
							text = is_owner_generic_check
							owner ?= root
						}
						custom_tooltip = {
							text = is_owner_generic_subject_check
							owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.9
				}
			}
		}
		
		duration = 0

		on_completion = {
			region:crete_region = {
				ordered_location_in_region = {
					limit = { owner ?= root }
					order_by = development
					max = 3
					add_core = root
				}
			}
			trigger_event_silently = venice_mission_events.5
		}
		#ai_will_do = { value = 100 }
	}

	mission_develop_candia = {
		icon = production_efficiency_advance
		requires = { mission_secure_crete }
		
		enabled = {
			location:iraklio = {
				owner ?= root
				development >= 25
				has_building_with_at_least_one_level = fortress
			}
		}
		
		duration = 0

		on_completion = {
			location:iraklio = {
				add_location_modifier = {
					modifier = candia_fortified
					years = 30
					mode = add
				}
				change_development = 3
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_establish_euboea_trade = {
		icon = merchants_and_trade
		requires = { mission_secure_crete }
		
		enabled = {
			location:chalkis = {
				owner ?= root
				has_building_with_at_least_one_level = marketplace
			}
			area:aegean_islands_area = {
				any_location_in_area = {
					owner ?= root
					has_building_with_at_least_one_level = wharf
					count >= 2
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = euboea_emporium
				years = 20
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_control_ionian_islands = {
		icon = maritime_advance_age_2
		requires = { mission_secure_crete }
		
		enabled = {
			area:ionian_islands_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
							text = is_owner_generic_check
							owner ?= root
						}
						custom_tooltip = {
							text = is_owner_generic_subject_check
							owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.8
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = ionian_outposts
				years = 25
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 4: ITALIAN MAINSTLAND EXPANSION ====================
	
	mission_acquire_terraferma = {
		icon = offensive_army
		requires = { mission_strengthen_republic }
		
		enabled = {
			area:veneto_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
							text = is_owner_generic_check
							owner ?= root
						}
						custom_tooltip = {
							text = is_owner_generic_subject_check
							owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.8
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_manpower = {
				value = max_manpower
				multiply = 0.25
			}
			trigger_event_silently = venice_mission_events.6
		}
		#ai_will_do = { value = 100 }
	}

	mission_secure_friuli = {
		icon = defensive_army
		requires = { mission_acquire_terraferma }
		
		enabled = {
			area:friuli_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
							text = is_owner_generic_check
							owner ?= root
						}
						custom_tooltip = {
							text = is_owner_generic_subject_check
							owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.7
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = alpine_border_secure
				years = 25
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_dominate_padua = {
		icon = cultural_acceptance_advance
		requires = { mission_acquire_terraferma }
		
		enabled = {
			location:padova = {
				owner ?= root
				development >= 30
				has_building_with_at_least_one_level = university
			}
		}
		
		duration = 0

		on_completion = {
			location:padova = {
				add_location_modifier = {
					modifier = paduan_intellectuals
					years = 30
					mode = add
				}
			}
			add_research_progress = research_progress_mild_bonus
		}
		#ai_will_do = { value = 100 }
	}

	mission_control_verona = {
		icon = offensive_army
		requires = { mission_acquire_terraferma }
		
		enabled = {
			location:verona = {
				owner ?= root
				has_building_with_at_least_one_level = castle
			}
		}
		
		duration = 0

		on_completion = {
			location:verona = {
				add_location_modifier = {
					modifier = veronese_stronghold
					years = 25
					mode = add
				}
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_secure_brescia = {
		icon = offensive_army
		requires = { mission_acquire_terraferma }
		
		enabled = {
			location:brescia = {
				owner ?= root
				development >= 25
			}
		}
		
		duration = 0

		on_completion = {
			area:lombardy_area = {
				random_location_in_area = {
					limit = { owner ?= root }
					add_location_modifier = {
						modifier = lombard_integration
						years = 20
						mode = add
					}
				}
			}
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 5: NAVAL SUPREMACY ====================
	
	mission_expand_arsenal = {
		icon = ship_building_advance
		requires = { mission_develop_arsenal }
		
		enabled = {
			capital = {
				total_building_levels:shipyard >= 2
				total_building_levels:dry_dock >= 3
			}
			navy_size >= 60
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = great_arsenal
					years = -1
					mode = add
				}
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_perfect_galley_design = {
		icon = ship_building_advance
		requires = { mission_expand_arsenal }
		
		enabled = {
			has_advance = galleass_advance
			navy_tradition >= 50
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = venetian_galley_tradition
				years = -1
				mode = add
			}
			add_navy_tradition = 20
		}
		#ai_will_do = { value = 100 }
	}

	mission_battle_of_lepanto_veterans = {
		icon = glorious_arms
		requires = { mission_perfect_galley_design }
		
		enabled = {
			navy_tradition >= 70
			OR = {
				has_completed_mission = mission_control_cyprus
				has_completed_mission = mission_secure_crete
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = lepanto_veterans
				years = 40
				mode = add
			}
			trigger_event_silently = venice_mission_events.7
		}
		#ai_will_do = { value = 100 }
	}

	mission_develop_merchant_marine = {
		icon = merchants_and_trade
		requires = { mission_expand_arsenal }
		
		enabled = {
			total_effective_building_levels:wharf >= 12
			monthly_income_trade_and_tax >= 200
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = venetian_merchant_fleet
				years = 30
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 6: TRADE MONOPOLIES ====================
	
	mission_monopolize_salt_trade = {
		icon = merchants_and_trade
		requires = { mission_develop_murano_glass }
		
		enabled = {
			any_owned_location = {
				raw_material = goods:salt
				count >= 8
			}
			produced_in_country:salt >= 30
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = salt_trade_monopoly
				years = 30
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_control_spice_route = {
		icon = merchants_and_trade
		requires = { mission_establish_levant_quarters }
		
		enabled = {
			any_owned_location = {
				raw_material = goods:chili
				count >= 4
			}
			capital.market = {
				"merchant_power_in_market(root)" >= 85
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = spice_route_dominance
				years = 35
				mode = add
			}
			trigger_event_silently = venice_mission_events.8
		}
		#ai_will_do = { value = 100 }
	}

	mission_establish_fondaco_dei_tedeschi = {
		icon = merchants_and_trade
		requires = { mission_monopolize_salt_trade }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = merchants_quarters
				has_building_with_at_least_one_level = marketplace
			}
			any_country = {
				culture.group = culture_group:germanic
				opinion = {
					target = root
					value >= 50
				}
				count >= 3
			}
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = fondaco_dei_tedeschi
					years = -1
					mode = add
				}
			}
			add_gold = 800
		}
		#ai_will_do = { value = 100 }
	}

	mission_establish_fondaco_dei_persiani = {
		icon = merchants_and_trade
		requires = { mission_control_spice_route }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = merchants_quarters
			}
			any_country = {
				religion.group = religion_group:muslim
				opinion = {
					target = root
					value >= 25
				}
				count >= 2
			}
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = fondaco_dei_persiani
					years = 30
					mode = add
				}
			}
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 7: RIVALRY WITH GENOA ====================
	
	mission_challenge_genoese_power = {
		icon = maritime_advance_age_2
		requires = { mission_dominate_levantine_trade }
		
		enabled = {
			country_exists = c:GEN
			root = {
				relative_strength = {
					target = c:GEN
					value > 0.75
				}
			}
			monthly_income_trade_and_tax >= var:mission_target_trade_income
		}
		
		duration = 0

		on_completion = {
			add_casus_belli = {
				target = c:GEN
				type = casus_belli:cb_trade_conflict
				years = 20
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_win_war_of_chiaoggia = {
		icon = glorious_arms
		requires = { mission_challenge_genoese_power }
		
		enabled = {
			OR = {
				NOT = { country_exists = c:GEN }
				c:GEN = {
					is_subject_of = root
				}
			}
			location:genova = {
				owner ?= root
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = genoese_rivalry_victory
				years = -1
				mode = add
			}
			trigger_event_silently = venice_mission_events.9
		}
		#ai_will_do = { value = 100 }
	}

	mission_control_corsica_sardinia = {
		icon = maritime_advance_age_2
		requires = { mission_win_war_of_chiaoggia }
		
		enabled = {
			region:corsica_sardinia_region = {
				any_location_in_region = {
					OR = {
						custom_tooltip = {
							text = is_owner_generic_check
							owner ?= root
						}
						custom_tooltip = {
							text = is_owner_generic_subject_check
							owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.7
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = western_mediterranean_presence
				years = 25
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 8: OTTOMAN CONFLICT BRANCH ====================
	
	mission_prepare_for_ottoman_threat = {
		icon = defensive_army
		requires = { mission_secure_crete }
		
		enabled = {
			country_exists = c:TUR
			region:balkan_region = {
				any_location_in_region = {
					owner ?= c:TUR
					count >= 10
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = ottoman_preparations
				years = 30
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_fortify_naupactus = {
		icon = defensive_army
		requires = { mission_prepare_for_ottoman_threat }
		
		enabled = {
			location:naupactus = {
				owner ?= root
				has_building_with_at_least_one_level = bastion
			}
		}
		
		duration = 0

		on_completion = {
			location:naupactus = {
				add_location_modifier = {
					modifier = naupactus_fortress
					years = 35
					mode = add
				}
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_defend_cyprus = {
		icon = defensive_army
		requires = { mission_control_cyprus mission_prepare_for_ottoman_threat }
		
		enabled = {
			region:cyprus_region = {
				any_location_in_region = {
					owner ?= root
					has_building_with_at_least_one_level = castle
					count >= 3
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = cyprus_fortified
				years = 30
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_defend_crete = {
		icon = defensive_army
		requires = { mission_develop_candia mission_prepare_for_ottoman_threat }
		
		enabled = {
			region:crete_region = {
				any_location_in_region = {
					owner ?= root
					has_building_with_at_least_one_level = castle
					count >= 3
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = crete_fortified
				years = 30
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_form_holy_league = {
		icon = diplomatic_influence
		requires = { mission_defend_cyprus mission_defend_crete }
		
		enabled = {
			religion = religion:catholic
			any_country = {
				religion = religion:catholic
				opinion = {
					target = root
					value >= 75
				}
				count >= 3
			}
			exists = c:PAP
			c:PAP ?= {
				opinion = {
					target = root
					value >= 75
				}
			}
		}
		
		duration = 0

		on_completion = {
			trigger_event_silently = venice_mission_events.10
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 9: CULTURAL RENAISSANCE ====================
	
	mission_patronize_venetian_art = {
		icon = cultural_acceptance_advance
		requires = { mission_strengthen_republic }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = art_academy
			}
			gold >= 2000
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = venetian_school_of_art
					years = 35
					mode = add
				}
			}
			add_prestige = 15
			trigger_event_silently = venice_mission_events.11
		}
		#ai_will_do = { value = 100 }
	}

	mission_commission_bellini_titian = {
		icon = cultural_acceptance_advance
		requires = { mission_patronize_venetian_art }
		
		enabled = {
			culture:venetian = {
				cultural_tradition >= 400
			}
			has_embraced_institution = institution:renaissance
		}
		
		duration = 0

		on_completion = {
			culture:venetian = {
				add_cultural_tradition = 50
			}
			trigger_event_silently = venice_mission_events.12
		}
		#ai_will_do = { value = 100 }
	}

	mission_establish_venetian_printing = {
		icon = technology_icon
		requires = { mission_patronize_venetian_art }
		
		enabled = {
			capital = {
				total_building_levels:printing_press_shop >= 3
			}
			average_country_literacy >= 25
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = venetian_publishing
				years = 35
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_st_mark_square = {
		icon = cultural_acceptance_advance
		requires = { mission_commission_bellini_titian }
		
		enabled = {
			capital = {
				has_building_with_at_least_one_level = grand_palace
				development >= 50
			}
		}
		
		duration = 0

		on_completion = {
			capital = {
				add_location_modifier = {
					modifier = piazza_san_marco
					years = -1
					mode = add
				}
			}
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 10: GOVERNMENT REFORMS ====================
	
	mission_limit_doge_power = {
		icon = government_reforms
		requires = { mission_strengthen_republic }
		
		enabled = {
			"estate_power(estate_type:nobles_estate)" <= 0.20
			republican_tradition >= 85
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = doge_limited
				years = 25
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_establish_council_of_ten = {
		icon = government_reforms
		requires = { mission_limit_doge_power }
		
		enabled = {
			has_government_reform = government_reform:venetian_council_of_ten
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = council_of_ten
				years = -1
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_create_state_inquisition = {
		icon = government_reforms
		requires = { mission_establish_council_of_ten }
		
		enabled = {
			any_owned_location = {
				has_building_with_at_least_one_level = courthouse
				count >= 8
			}
			stability >= 70
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = venetian_inquisition
				years = 30
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 11: EXPANSION INTO GREECE ====================
	
	mission_secure_methoni_koroni = {
		icon = maritime_advance_age_2
		requires = { mission_control_ionian_islands }
		
		enabled = {
			location:methoni = {
				owner ?= root
			}
			location:koroni = {
				owner ?= root
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = eyes_of_venice
				years = 25
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_acquire_monemvasia = {
		icon = glorious_arms
		requires = { mission_secure_methoni_koroni }
		
		enabled = {
			location:monemvasia = {
				owner ?= root
			}
		}
		
		duration = 0

		on_completion = {
			location:monemvasia = {
				add_location_modifier = {
					modifier = malvasia_fortress
					years = 25
					mode = add
				}
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_control_thessaloniki = {
		icon = offensive_army
		requires = { mission_secure_methoni_koroni }
		
		enabled = {
			location:thessaloniki = {
				owner ?= root
			}
		}
		
		duration = 0

		on_completion = {
			location:thessaloniki = {
				add_core = root
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_venetian_athens = {
		icon = cultural_acceptance_advance
		requires = { mission_control_thessaloniki }
		
		enabled = {
			location:athens = {
				owner ?= root
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = venetian_duchy_of_athens
				years = 25
				mode = add
			}
			add_prestige = 15
			trigger_event_silently = venice_mission_events.13
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 12: ECONOMIC REFINEMENT ====================
	
	mission_establish_venetian_banking = {
		icon = court_accounting
		requires = { mission_establish_fondaco_dei_tedeschi }
		
		enabled = {
			has_advance = banking_advance
			gold >= 3000
			capital = {
				has_building_with_at_least_one_level = banking_office
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = venetian_banking_house
				years = 35
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	mission_create_public_debt = {
		icon = court_accounting
		requires = { mission_establish_venetian_banking }
		
		enabled = {
			monthly_income_trade_and_tax >= 300
			interest_rate <= 0.03
		}
		
		duration = 0

		on_completion = {
			add_gold = 3000
		}
		#ai_will_do = { value = 100 }
	}

	mission_standardize_ducat = {
		icon = merchants_and_trade
		requires = { mission_establish_venetian_banking }
		
		enabled = {
			capital.market = {
				"merchant_power_in_market(root)" >= 90
			}
			trade_efficiency >= 0.25
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = venetian_ducat_standard
				years = 40
				mode = add
			}
		}
		#ai_will_do = { value = 100 }
	}

	# ==================== PHASE 13: FINAL ULTIMATE MISSIONS ====================
	
	mission_venetian_thalassocracy = {
		icon = empire_icon
		requires = { 
			mission_win_war_of_chiaoggia
			mission_perfect_galley_design
			mission_control_spice_route
		}
		
		enabled = {
			is_great_power = yes
			navy_tradition >= 80
			navy_size >= 150
			monthly_income_trade_and_tax >= 500
			region:mediterranean_region = {
				any_sea_zone_in_region = {
					naval_supremacy >= 0.6
					count >= 8
				}
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = venetian_thalassocracy
				years = -1
				mode = add
			}
			trigger_event_silently = venice_mission_events.14
		}
		#ai_will_do = { value = 100 }
	}

	mission_most_serene_republic = {
		icon = empire_icon
		requires = { 
			mission_venetian_thalassocracy
			mission_st_mark_square
			mission_establish_council_of_ten
		}
		final = yes
		
		enabled = {
			is_great_power = yes
			republican_tradition >= 90
			stability >= 90
			prestige >= 90
			total_development >= 4000
			num_subjects >= 5
			capital = {
				development >= 60
			}
		}
		
		duration = 0

		on_completion = {
			add_country_modifier = {
				modifier = most_serene_republic
				years = -1
				mode = add
			}
			add_republican_tradition = 50
			trigger_event_silently = venice_mission_events.15
		}
		ai_will_do = { value = -100 }
	}
}