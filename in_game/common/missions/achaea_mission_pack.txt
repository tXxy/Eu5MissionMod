achaea_missions = {
    icon = generic_capital_economy
    repeatable = no
    chance = 3600   
    player_playstyle = diplomatic
    # ai_will_do = { value = 100 }   

    visible = {
        game_has_missions_enabled = yes
        OR = {
            has_or_had_tag = ACH
            has_or_had_tag = ATH
            has_or_had_tag = LAT
        }
    }

    on_start = { 
        add_country_modifier = {
            modifier = ach_latin_legacy
            days = -1
            mode = add_and_extend
        }
    }

    on_abort = {
        remove_country_modifier = ach_latin_legacy
    }

    on_completion = {
        add_prestige = 50
        add_legitimacy = 25
    }

# ==================== INDEPENDENCE & INTERNAL POLITICS BRANCH ====================

    mission_ach_strengthen_the_duchy = {
        icon = crown_power_advance_renaissance
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            stability >= 30
            legitimacy >= 30
            "estate_power(estate_type:crown_estate)" >= 0.15
            location:andravida = {
                local_control >= 0.75
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_ducal_authority
                years = 15
                mode = add_and_extend
            }
            add_stability = 5
        }
    }

    mission_ach_secure_the_baronies = {
        icon = hofkriegsrat
        requires = { mission_ach_strengthen_the_duchy }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            "estate_power(estate_type:crown_estate)" >= 0.20
            location:andravida = {
                local_control >= 0.55
            }
            location:chalandritsa = {
                local_control >= 0.50
            }
            "estate_power(estate_type:nobles_estate)" <= 0.40
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_baronial_consolidation
                years = 20
                mode = add_and_extend
            }
            add_prestige = 10
        }
    }

    mission_ach_shadow_of_naples = {
        icon = diplomatic_influence
        requires = { mission_ach_strengthen_the_duchy }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
            is_subject_of = c:NAP
        }

        bypass = {
            NOT = {
                is_subject_of = c:NAP
            }
        }

        enabled = {
            favors = {
                target = c:NAP
                value >= 10
            }
            spy_network = {
                target ?= c:NAP
                value >= 30
            }
            gold >= 200
        }

        on_completion = {
            c:NAP = {
                add_opinion = {
                    target = root
                    modifier = opinion_ach_independence_ambition
                }
            }
            add_liberty_desire = 25
						add_legitimacy = 30
        }
    }

    mission_ach_assert_independence = {
        icon = pal_a_just_ruler
        requires = { 
            mission_ach_shadow_of_naples
            mission_ach_secure_the_baronies
        }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            NOT = { is_subject = yes }
            stability >= 40
            legitimacy >= 50
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_independence_secured
                years = -1
                mode = add_and_extend
            }
            add_prestige = 30
        }
    }

    mission_ach_establish_parliament = {
        icon = monarchy_reform_traditions_advance
        requires = { mission_ach_assert_independence }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            "estate_power(estate_type:nobles_estate)" >= 0.20
            "estate_power(estate_type:burghers_estate)" >= 0.15
            legitimacy >= 60
            stability >= 50
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_parliament_established
                years = 20
                mode = add_and_extend
            }
            add_legitimacy = 15
        }
    }

    mission_ach_reform_feudal_contracts = {
        icon = legalism_advance
        requires = { mission_ach_establish_parliament }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            has_estate_privilege = estate_privilege:lat_latin_crusader_elite
            "estate_power(estate_type:nobles_estate)" <= 0.35
            estate_satisfaction:nobles_estate >= 0.55
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_reformed_feudalism
                years = 25
                mode = add_and_extend
            }
            add_estate_satisfaction = { type = estate_type:burghers_estate value = estate_satisfaction_mild_bonus }
        }
    }

    mission_ach_deal_with_the_anjevins = {
        icon = diplomatic_training_diplomatic_capacity
        requires = { mission_ach_assert_independence }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
					c:NAP = {
							opinion = {
									target = root
									value >= 100
							}
					}
					total_merchant_capacity >= 50
					army_size_percentage >= 0.80
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_anjevins_dealt_with
                years = 15
                mode = add_and_extend
            }
        }
    }

# ==================== ECONOMIC DEVELOPMENT BRANCH ====================

    mission_ach_develop_the_morea = {
        icon = agriculture_advance
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            area:morea_area = {
                any_location_in_area = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.70
                }
            }
            stability >= 25
        }

        on_completion = {
            area:morea_area = {
                ordered_location_in_area = {
                    limit = { is_owned_or_owned_by_subjects_of = root }
                    order_by = population
                    max = 5
                    change_development = 2
										add_core = root
                }
            }
            add_country_modifier = {
                modifier = ach_morean_development
                years = 20
                mode = add_and_extend
            }
        }
    }

    mission_ach_cultivate_greek_wine = {
        icon = pal_cultivate_vineyards
        requires = { mission_ach_develop_the_morea }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            any_owned_location = {
                raw_material = goods:wine
                count >= 3
            }
            produced_in_country:wine >= 12
        }

        on_completion = {
            ordered_owned_location = {
                limit = {
                    raw_material = goods:wine
                    is_full_expanded_rgo = no
                }
                order_by = population
                max = 3
                change_max_raw_material_workers = 1
            }
            every_owned_location = {
                limit = { raw_material = goods:wine }
                add_location_modifier = {
                    modifier = ach_morean_wine
                    years = -1
                    mode = add_and_extend
                }
            }
        }
    }

    mission_ach_olive_groves_of_the_peloponnese = {
        icon = food_advance_renaissance
        requires = { mission_ach_develop_the_morea }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            any_owned_location = {
                raw_material = goods:olives
                count >= 4
            }
            produced_in_country:olives >= 16
        }

        on_completion = {
            ordered_owned_location = {
                limit = {
                    raw_material = goods:olives
                    is_full_expanded_rgo = no
                }
                order_by = population
                max = 4
                change_max_raw_material_workers = 1
            }
            every_owned_location = {
                limit = { raw_material = goods:olives }
                add_location_modifier = {
                    modifier = ach_peloponnesian_olive_oil
                    years = -1
                    mode = add_and_extend
                }
            }
            add_estate_satisfaction = { type = estate_type:peasants_estate value = estate_satisfaction_mild_bonus }
        }
    }

    mission_ach_silk_of_the_morea = {
        icon = jap_reformed_land_holding
        requires = { mission_ach_develop_the_morea }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            any_owned_location = {
                raw_material = goods:silk
                count >= 2
            }
            produced_in_country:silk >= 8
        }

        on_completion = {
            every_owned_location = {
                limit = { raw_material = goods:silk }
                add_location_modifier = {
                    modifier = ach_morean_silk
                    years = -1
                    mode = add_and_extend
                }
            }
            add_estate_satisfaction = { type = estate_type:burghers_estate value = estate_satisfaction_mild_bonus }
        }
    }

    mission_ach_establish_trade_routes = {
        icon = merchants_and_trade
        requires = { 
            mission_ach_cultivate_greek_wine
            mission_ach_olive_groves_of_the_peloponnese
        }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            num_of_trades >= 5
            total_merchant_capacity >= 25
            monthly_income_trade_and_tax >= 50
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_trade_routes_established
                years = 25
                mode = add_and_extend
            }
            add_estate_satisfaction = { type = estate_type:burghers_estate value = estate_satisfaction_mild_bonus }
        }
    }

    mission_ach_patras_emporium = {
        icon = reduce_inflations_advance
        requires = { mission_ach_establish_trade_routes }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            location:patras = {
                is_owned_or_owned_by_subjects_of = root
                has_building_with_at_least_one_level = marketplace
                development >= 21
            }
        }

        on_completion = {
            location:patras = {
                add_location_modifier = {
                    modifier = ach_patras_trade_hub
                    years = 25
                    mode = add_and_extend
                }
                change_building_level_in_location = {
                    building = building_type:marketplace
                    value = 1
                }
            }
            add_estate_satisfaction = { type = estate_type:burghers_estate value = estate_satisfaction_mild_bonus }
        }
    }

    mission_ach_dominate_corinthian_gulf_trade = {
        icon = global_trade_routes_advance
        requires = { mission_ach_patras_emporium }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            owns = location:corinth
            location:corinth = {
                # is_market_center = yes
                has_building_with_at_least_one_level = market_warehouse
            }
            location:corinth.market = {
                total_goods_value_traded >= 1000
            }
        }

        on_completion = {
            location:corinth = {
                add_location_modifier = {
                    modifier = ach_gulf_of_corinth_trade
                    years = 25
                    mode = add_and_extend
                }
            }
            add_gold = 300
            add_navy_tradition = 5
        }
    }

    mission_ach_achaean_merchant_marine = {
        icon = maritime_advance_age_1
        requires = { mission_ach_dominate_corinthian_gulf_trade }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
					  custom_tooltip = {
							text = ach_local_maritime_presence_tt
							any_port_in_country = {
									location_maritime_presence_power = {
											country = root
											value >= 30
									}
							}
            }
            num_of_trades >= 8
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_achaean_merchant_marine
                years = 25
                mode = add_and_extend
            }
        }
    }

# ==================== NAVAL DEVELOPMENT BRANCH ====================

    mission_ach_build_a_navy = {
        icon = maritime_advance_age_2
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            navy_size >= 5
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_naval_buildup
                years = 25
                mode = add_and_extend
            }
        }
    }

    mission_ach_expand_the_galley_fleet = {
        icon = nav_shipyards_ironworks
        requires = { mission_ach_build_a_navy }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            navy_size >= 15
            total_sub_unit_count:navy_galley >= 8
            # any_owned_location = {
            #     has_building_with_at_least_one_level = dry_dock
            #     count >= 2
            # }
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_galley_fleet_expanded
                years = 25
                mode = add_and_extend
            }
            add_sailors = {
                value = root.monthly_sailors
                multiply = 6
            }
        }
    }

    mission_ach_control_the_ionian_sea = {
        icon = nav_always_ready
        requires = { mission_ach_expand_the_galley_fleet }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
						owns = location:corfu
						owns = location:cephalonia
						owns = location:leukas
						owns = location:zakynthos
            navy_size_percentage >= 0.70
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_ionian_control
                years = 15
                mode = add_and_extend
            }

						location:corfu = {
							limit = { is_owned_or_owned_by_subjects_of = root }
							change_integration_progress = 50
						}
						location:cephalonia = {
							limit = { is_owned_or_owned_by_subjects_of = root }
							change_integration_progress = 50
						}
						location:leukas = {
							limit = { is_owned_or_owned_by_subjects_of = root }
							change_integration_progress = 50
						}
						location:zakynthos = {
							limit = { is_owned_or_owned_by_subjects_of = root }
							change_integration_progress = 50
						}
        }
    }

    mission_ach_secure_the_aegean_sea = {
        icon = maritime_advance_age_4
        requires = { mission_ach_control_the_ionian_sea }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            navy_size >= 25
            area:aegean_sea_area = {
                any_location_in_area = {
                    location_maritime_presence_power = {
                        country = root
                        value >= 30
                    }
                    count >= 5
                }
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_aegean_naval_dominance
                years = 30
                mode = add_and_extend
            }
            add_navy_tradition = 15
        }
    }

    mission_ach_crete_and_cyprus = {
        icon = austrian_military_flexibility
        requires = { mission_ach_secure_the_aegean_sea }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            province_definition:crete_province = {
                any_location_in_province_definition = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.60
                }
            }
            province_definition:cyprus_province = {
                any_location_in_province_definition = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.50
                }
            }
						# own_entire_province = province_definition:cyprus_province 
        }

        on_completion = {
            province_definition:crete_province = {
                every_location_in_province_definition = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
										add_core = root
                }
            }
            province_definition:cyprus_province = {
                every_location_in_province_definition = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
										add_core = root
                }
            }
            add_country_modifier = {
                modifier = ach_crete_cyprus_outposts
                years = 15
                mode = add_and_extend
            }
        }
    }

    mission_ach_establish_maritime_trade = {
        icon = global_trade_advance
        requires = { 
            mission_ach_secure_the_aegean_sea
            mission_ach_achaean_merchant_marine
        }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            monthly_income_trade_and_tax >= 150
            num_of_trades >= 8
            any_market_center_in_country = {
                total_goods_value_traded >= 1500
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_maritime_trade_empire
                years = -1
                mode = add_and_extend
            }
        }
    }

# ==================== MILITARY EXPANSION BRANCH ====================

    mission_ach_prepare_for_expansion = {
        icon = drill_army_advance
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            army_size_percentage >= 0.80
            war_exhaustion < 1
            gold >= 200
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_expansion_ready
                years = 15
                mode = add_and_extend
            }
        }
    }

    mission_ach_unify_the_morea = {
        icon = northern_march
        requires = { mission_ach_prepare_for_expansion }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            area:morea_area = {
				any_location_in_area = {
					OR = {
						custom_tooltip = {
							text = is_owner_generic_check
							owner ?= root
						}
						custom_tooltip = {
							text = is_owner_generic_subject_check
							owner ?= { is_subject_of = root }
						}
					}
					percent >= 0.85
				}
            }
        }

        on_completion = {
            area:morea_area = {
                every_location_in_area = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
                    add_core = root
                    change_integration_progress = 0.75
                }
            }
            add_country_modifier = {
                modifier = ach_morea_unified
                years = 15
                mode = add_and_extend
            }
        }
    }

    mission_ach_conquer_attica = {
        icon = glorious_arms
        requires = { mission_ach_unify_the_morea }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            province_definition:attica_province = {
                any_location_in_province_definition = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.80
                }
            }
            owns = location:athens
        }

        on_completion = {
            location:athens = {
                add_location_modifier = {
                    modifier = ach_athens_conquered
                    years = -1
                    mode = add_and_extend
                }
            }
            province_definition:attica_province = {
                ordered_location_in_province_definition = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
                    order_by = development
                    max = 3
                    add_core = root
                }
            }
        }
    }

    mission_ach_claim_the_greek_islands = {
        icon = maritime_advance_age_2
        requires = { mission_ach_prepare_for_expansion }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            province_definition:naxos_province = {
                any_location_in_province_definition = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.60
                }
            }
            navy_size >= 20
        }

        on_completion = {
            province_definition:naxos_province = {
                every_location_in_province_definition = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
                    add_core = root
                }
            }
            add_country_modifier = {
                modifier = ach_islands_secured
                years = 25
                mode = add_and_extend
            }
        }
    }

    mission_ach_epirus_campaign = {
        icon = defensive_army
        requires = { mission_ach_unify_the_morea }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            province_definition:south_epirus_province = {
                any_location_in_province_definition = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.60
                }
            }
            any_owned_location = {
                count >= 3
                has_building_with_at_least_one_level = castle
            }
        }

        on_completion = {
            province_definition:south_epirus_province = {
                ordered_location_in_province_definition = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
                    order_by = development
                    max = 4
                    add_core = root
                }
            }
            add_country_modifier = {
                modifier = ach_epirus_expansion
                years = 20
                mode = add_and_extend
            }
						add_stability = 5
        }
    }

    mission_ach_albanian_marches = {
        icon = hiberno_norman_keeps_advance
        requires = { mission_ach_epirus_campaign }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            area:albania_area = {
                any_location_in_area = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.50
                }
            }
        }

        on_completion = {
            area:albania_area = {
                ordered_location_in_area = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
                    order_by = development
                    max = 4
                    add_core = root
                }
            }
						add_gold = 200
						add_legitimacy = 20
        }
    }

    mission_ach_conquer_thessaly = {
        icon = glorious_arms
        requires = { mission_ach_epirus_campaign mission_ach_conquer_attica }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            province_definition:thessaly_province = {
                any_location_in_province_definition = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.70
                }
            }
        }

        on_completion = {
            province_definition:thessaly_province = {
                every_location_in_province_definition = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
                    add_core = root
                }
            }
            add_manpower = {
                value = max_manpower
                multiply = 0.25
            }
        }
    }

    mission_ach_conquer_macedonia = {
        icon = glorious_arms
        requires = { mission_ach_conquer_thessaly }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            area:macedonia_area = {
                any_location_in_area = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.70
                }
            }
            owns = location:thessaloniki
        }

        on_completion = {
            area:macedonia_area = {
                every_location_in_area = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
                    add_core = root
                }
            }
            location:thessaloniki = {
                add_location_modifier = {
                    modifier = ach_thessaloniki_secured
                    years = -1
                    mode = add_and_extend
                }
            }
            add_country_modifier = {
                modifier = ach_macedonia_conquered
                years = 15
                mode = add_and_extend
            }
        }
    }

    mission_ach_conquer_thrace = {
        icon = glorious_arms
        requires = { mission_ach_conquer_macedonia }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            area:thrace_area = {
                any_location_in_area = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.70
                }
            }
        }

        on_completion = {
            area:thrace_area = {
                every_location_in_area = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
                    add_core = root
										change_prosperity = prosperity_mild_bonus
                }
            }
            add_country_modifier = {
                modifier = ach_thrace_conquered
                years = 15
                mode = add_and_extend
            }
        }
    }

    mission_ach_capture_constantinople = {
        icon = empire_icon
        requires = { mission_ach_conquer_thrace }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            owns = location:constantinople
            location:constantinople = {
                development >= 30
                local_control >= 0.60
            }
        }

        on_completion = {
            location:constantinople = {
                add_location_modifier = {
                    modifier = ach_latin_conquest
                    years = -1
                    mode = add_and_extend
                }
                change_development = 3
                change_integration_progress = 75
								add_core = root
            }
            add_legitimacy = 30
        }
    }

# ==================== RELIGIOUS & CULTURAL BRANCH ====================

    mission_ach_catholic_mission = {
        icon = preaching_friars
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            religion = religion:catholic
            "estate_power(estate_type:clergy_estate)" >= 0.15
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_catholic_revival
                years = 25
                mode = add_and_extend
            }
            add_religious_influence = 30
        }
    }

    mission_ach_latin_patronage = {
        icon = ties_with_the_holy_see
        requires = { mission_ach_catholic_mission }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            c:PAP = {
                opinion = {
                    target = root
                    value >= 150
                }
            }
            religious_unity >= 0.50
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_latin_patronage
                years = 25
                mode = add_and_extend
            }
            add_religious_influence = 20
        }
    }

    mission_ach_convert_the_orthodox = {
        icon = religious_unity_advance
        requires = { mission_ach_catholic_mission }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            religion = religion:catholic
            religious_unity >= 0.75
            stability >= 25
            "estate_power(estate_type:clergy_estate)" <= 0.25
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_orthodox_subjugation
                years = 30
                mode = add_and_extend
            }
            add_estate_satisfaction = { type = estate_type:clergy_estate value = estate_satisfaction_mild_bonus }
        }
    }

    mission_ach_promote_latin_culture = {
        icon = cultural_acceptance_advance
        requires = { mission_ach_catholic_mission }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            culture:greek_culture = {
                is_accepted_in = root
            }
            # culture:roman_culture = {
            #     is_accepted_in = root
            # }
            any_owned_location = {
                count >= 3
                has_building_with_at_least_one_level = library
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_latin_cultural_revival
                years = 25
                mode = add_and_extend
            }
						add_stability = 5
        }
    }

    mission_ach_frankish_greek_fusion = {
        icon = cultural_ties
        requires = { 
            mission_ach_promote_latin_culture
            mission_ach_convert_the_orthodox
        }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            culture_percentage_in_country = {
                culture = culture:greek_culture
                value >= 0.25
            }
						culture_percentage_in_country = {
                culture = culture:french
                value >= 0.25
            }
            estate_satisfaction:peasants_estate >= 0.55
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_frankish_greek_fusion
                years = -1
                mode = add_and_extend
            }
            culture:french = {
                change_cultural_view = {
                    target = culture:greek_culture
                    change = 1
                }
            }
            culture:greek_culture = {
                change_cultural_view = {
                    target = culture:french
                    change = 1
                }
            }
        }
    }

    mission_ach_establish_latin_patriarchate = {
        icon = pap_the_word_of_the_lord
        requires = { 
            mission_ach_convert_the_orthodox
            mission_ach_latin_patronage
        }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            religion = religion:catholic
            owns = location:constantinople
            c:PAP = {
                opinion = {
                    target = root
                    value >= 120
                }
            }
        }

        on_completion = {
						add_legitimacy = 20
						add_stability = 10
            add_religious_influence = 50
        }
    }

# ==================== MILITARY MODERNIZATION BRANCH ====================

    mission_ach_restore_the_theme_system = {
        icon = recruitment_improvements_renaissance
        requires = { mission_ach_unify_the_morea }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            area:morea_area = {
                any_location_in_area = {
                    has_building_with_at_least_one_level = armory
                    count >= 3
                }
            }
            army_size >= 15
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_theme_system_restored
                years = 30
                mode = add_and_extend
            }
            add_manpower = {
                value = max_manpower
                multiply = 0.30
            }
        }
    }

    mission_ach_fortify_the_hexamilion = {
        icon = defensive_army
        requires = { mission_ach_unify_the_morea }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            location:corinth = {
                is_owned_or_owned_by_subjects_of = root
                has_building_with_at_least_one_level = castle
                development >= 25
            }
        }

        on_completion = {
            location:corinth = {
                add_location_modifier = {
                    modifier = ach_hexamilion_wall
                    years = -1
                    mode = add_and_extend
                }
            }
        }
    }

    mission_ach_modernize_the_army = {
        icon = military_revolution_advance
        requires = { mission_ach_restore_the_theme_system }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            has_embraced_institution = institution:military_revolution
            any_owned_location = {
                has_building_with_at_least_one_level = barracks
                count >= 5
            }
            any_owned_location = {
                has_building_with_at_least_one_level = cannon_workshop
                count >= 3
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_modern_army
                years = 20
                mode = add_and_extend
            }
            add_army_tradition = 15
        }
    }

    mission_ach_black_powder_politics = {
        icon = gunpowder_advance
        requires = { mission_ach_modernize_the_army }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            produced_in_country:iron >= 10
            any_owned_location = {
                has_building_with_at_least_one_level = cannon_workshop
                count >= 6
            }
            army_tradition >= 30
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_black_powder_politics
                years = 20
                mode = add_and_extend
            }
        }
    }

# ==================== FORMATION OF THE LATIN EMPIRE ====================

    mission_ach_claim_the_legacy_of_rome = {
        icon = imperial_ambitions
        requires = { 
            mission_ach_capture_constantinople
            mission_ach_establish_latin_patriarchate
        }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            owns = location:constantinople
            legitimacy >= 85
            area:thrace_area = {
                any_location_in_area = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.75
                }
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_roman_legacy_claimed
                years = 30
                mode = add_and_extend
            }
        }
    }

    mission_ach_reclaim_anatolia = {
        icon = glorious_arms
        requires = { mission_ach_claim_the_legacy_of_rome }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            region:anatolia_region = {
                any_location_in_region = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.35
                }
            }
            army_size >= 30
        }

        on_completion = {
            region:anatolia_region = {
                ordered_location_in_region = {
                    limit = { is_owned_or_owned_by_subjects_of = root NOT = { is_core_of = root } }
                    order_by = development
                    max = 8
                    add_core = root
                }
            }
            add_country_modifier = {
                modifier = ach_anatolia_reclaimed
                years = 20
                mode = add_and_extend
            }
        }
    }

    mission_ach_subdue_the_turkish_beys = {
        icon = cavalry_advance
        requires = { mission_ach_reclaim_anatolia }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            region:anatolia_region = {
                any_location_in_region = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.75
                }
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_turkish_beys_subdued
                years = 15
                mode = add_and_extend
            }
            add_manpower = {
                value = max_manpower
                multiply = 0.50
            }
        }
    }

    mission_ach_prepare_for_imperial_coronation = {
        icon = crown_power_advance_renaissance
        requires = { 
            mission_ach_subdue_the_turkish_beys
        }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            is_great_power = yes
            legitimacy >= 90
            army_size >= 40
            navy_size >= 25
            owns = location:constantinople
            NOT = { has_any_active_disaster = yes }
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_latin_empire_established
                years = -1
                mode = add_and_extend
            }
        }
    }

# ==================== LATIN EMPIRE CONTENT (POST-FORMATION) ====================
# These missions become visible/available after forming the Latin Empire

		mission_lat_form_the_latin_empire = {
				icon = crown_power_advance_renaissance
				# ai_will_do = { value = 100 }
				duration = 0

				enabled = {
					has_or_had_tag = LAT
				}

				on_completion = {
					add_country_modifier = {
							modifier = lat_empire_formed
							years = -1
							mode = add_and_extend
					}
				}
		}

    mission_lat_imperial_authority = {
        icon = crown_power_advance_renaissance
        # ai_will_do = { value = 100 }
        duration = 0

        requires = { mission_lat_form_the_latin_empire }

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            legitimacy >= 75
            stability >= 75
            "estate_power(estate_type:crown_estate)" >= 0.30
            "estate_power(estate_type:nobles_estate)" <= 0.35
            owns = location:constantinople
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_imperial_authority
                years = 25
                mode = add_and_extend
            }
        }
    }

    mission_lat_imperial_bureaucracy = {
        icon = legalism_advance
        requires = { mission_lat_imperial_authority }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            average_control_in_home_region >= 0.75
            average_country_literacy >= 25
            any_owned_location = {
                has_building_with_at_least_one_level = counting_house
                count >= 6
            }
        }

        on_completion = {
            add_gold = 750
						add_stability = 15
						add_legitimacy = 15
						add_research_progress = research_progress_severe_bonus
        }
    }

    mission_lat_restore_the_senate = {
        icon = monarchy_reform_traditions_advance
        requires = { mission_lat_imperial_bureaucracy }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            estate_satisfaction:burghers_estate >= 0.60
            estate_satisfaction:nobles_estate >= 0.60
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_senate_restored
                years = 15
                mode = add_and_extend
            }
        }
    }

    mission_lat_crusade_for_the_holy_land = {
        icon = crusade_icon
        requires = { 
            mission_ach_subdue_the_turkish_beys
        }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            religion = religion:catholic
            army_size >= 35
            c:PAP = {
                opinion = {
                    target = root
                    value >= 120
                }
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_crusader_state
                years = 30
                mode = add_and_extend
            }
            add_casus_belli = {
                type = casus_belli:cb_crusade
                target = location:jerusalem.owner
                years = 25
            }
        }
    }

    mission_lat_capture_jerusalem = {
        icon = crusade_icon
        requires = { mission_lat_crusade_for_the_holy_land }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            owns = location:jerusalem
            location:jerusalem = {
                local_control >= 0.50
            }
        }

        on_completion = {
            location:jerusalem = {
                add_location_modifier = {
                    modifier = lat_jerusalem_restored
                    years = -1
                    mode = add_and_extend
                }
                change_prosperity = 0.25
                change_integration_progress = 0.75
            }
            add_religious_influence = 15
        }
    }

    mission_lat_convert_the_levant = {
        icon = religious_unity_advance
        requires = { mission_lat_capture_jerusalem }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            religion = religion:catholic
            area:levant_area = {
                any_location_in_area = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.40
                }
                religion_percentage = {
                    religion = religion:catholic
                    value >= 0.40
                }
            }
            "estate_power(estate_type:clergy_estate)" >= 0.20
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_catholic_levant
                years = -1
                mode = add_and_extend
            }

						area:levant_area = {
							ordered_location_in_area = {
								limit = {
									is_owned_or_owned_by_subjects_of = root 
								}
								order_by = population
								max = 5
								every_pop = {
									limit = { religion != religion:catholic }
									change_pop_religion = religion:catholic
								}
							}
            }
        }
    }

    mission_lat_convert_the_orthodox_empire = {
        icon = religious_unity_advance
        requires = { mission_ach_establish_latin_patriarchate }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            religion = religion:catholic
            religious_unity >= 0.80
            area:thrace_area = {
                religion_percentage = {
                    religion = religion:catholic
                    value >= 0.60
                }
            }
            region:anatolia_region = {
                religion_percentage = {
                    religion = religion:catholic
                    value >= 0.40
                }
            }
            stability >= 40
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_catholic_empire
                years = -1
                mode = add_and_extend
            }
            add_religious_influence = 50
            add_stability = 20
        }
    }

    mission_lat_unite_catholic_greece = {
        icon = religious_unity_advance
        requires = { mission_lat_convert_the_orthodox_empire }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            religion = religion:catholic
            religious_unity >= 0.95
            c:PAP = {
                opinion = {
                    target = root
                    value >= 150
                }
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_catholic_unity
                years = -1
                mode = add_and_extend
            }
            add_religious_influence = 30
            add_prestige = 30
        }
    }

    mission_lat_imperial_army_reforms = {
        icon = military_revolution_advance
        requires = { mission_ach_modernize_the_army }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            army_tradition >= 50
            army_size >= 50
            any_owned_location = {
                has_building_with_at_least_one_level = barracks
                count >= 15
            }
            any_owned_location = {
                has_building_with_at_least_one_level = cannon_workshop
                count >= 8
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_imperial_legions
                years = -1
                mode = add_and_extend
            }
        }
    }

    mission_lat_imperial_navy_reforms = {
        icon = maritime_advance_age_4
        requires = { mission_ach_secure_the_aegean_sea }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            navy_size >= 40
            navy_tradition >= 50
            any_owned_location = {
                has_building_with_at_least_one_level = shipyard
                count >= 6
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_imperial_navy
                years = 25
                mode = add_and_extend
            }
            add_sailors = {
                value = root.monthly_sailors
                multiply = 12
            }
        }
    }

    mission_lat_trade_hegemony = {
        icon = global_trade_advance
        requires = { 
            mission_ach_establish_maritime_trade
            mission_lat_imperial_navy_reforms
        }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            monthly_income_trade_and_tax >= 400
            num_of_trades >= 12
            capital.market = {
                total_goods_value_traded >= 3000
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_trade_hegemony
                years = 25
                mode = add_and_extend
            }
        }
    }

    mission_lat_unite_the_crusader_kingdoms = {
        icon = crusade_icon
        requires = { 
            mission_lat_capture_jerusalem
						mission_lat_imperial_authority
        }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            owns = location:constantinople
            owns = location:jerusalem
            owns = location:nicosia_cyp
            area:levant_area= {
                any_location_in_area = {
                    is_owned_or_owned_by_subjects_of = root
                    percent >= 0.60
                }
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_crusader_unity
                years = 30
                mode = add_and_extend
            }
            add_legitimacy = 30
        }
    }

    mission_lat_dominate_the_mediterranean = {
        icon = maritime_advance_age_4
        requires = { 
            mission_lat_imperial_navy_reforms
            mission_lat_trade_hegemony
        }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            is_hegemon_type = hegemony:naval_hegemon
            navy_size >= 60
            num_of_trades >= 15
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_mediterranean_dominance
                years = -1
                mode = add_and_extend
            }
        }
    }

    mission_lat_reconquer_roman_empire = {
        icon = imperial_ambitions
        requires = { 
            mission_lat_imperial_army_reforms
            mission_lat_convert_the_orthodox_empire
        }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            is_hegemon_type = hegemony:military_hegemon
            is_great_power = yes
            army_size >= 60
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_roman_empire_restored
                years = -1
                mode = add_and_extend
            }
        }
    }

    mission_ach_establish_the_academy = {
        icon = university_naples
        requires = { mission_ach_promote_latin_culture }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            capital = {
                has_building_with_at_least_one_level = university
                development >= 25
            }
            average_country_literacy >= 20
            has_embraced_institution = institution:renaissance
        }

        on_completion = {
            capital = {
                add_location_modifier = {
                    modifier = ach_ducal_academy
                    years = -1
                    mode = add_and_extend
                }
            }
            add_research_progress = research_progress_severe_bonus
        }
    }

    mission_ach_prince_of_achaea = {
        icon = bill_of_rights
        requires = { 
            mission_ach_deal_with_the_anjevins
            mission_ach_reform_feudal_contracts
        }
        # ai_will_do = { value = 100 }
        duration = 0

        enabled = {
            country_rank_level >= 3
            great_power_ranking <= 5
        }

        on_completion = {
            add_country_modifier = {
                modifier = ach_prince_of_achaea
                years = 20
                mode = add_and_extend
            }
            add_legitimacy = 30
            add_prestige = 25
        }
    }

    mission_lat_imperial_coronation_ceremony = {
        icon = crown_power_advance_renaissance
        requires = { 
            mission_ach_prepare_for_imperial_coronation
        }
        # ai_will_do = { value = 100 }
        duration = 0

        visible = {
					has_or_had_tag = LAT
        }

        enabled = {
            owns = location:constantinople
            location:constantinople = {
                has_building_with_at_least_one_level = grand_palace
                development >= 50
            }
            prestige >= 90
            legitimacy >= 95
            c:PAP = {
                opinion = {
                    target = root
                    value >= 150
                }
            }
        }

        on_completion = {
            add_country_modifier = {
                modifier = lat_imperial_coronation
                years = -1
                mode = add_and_extend
            }
            add_prestige = 75
            add_legitimacy = 50
            add_stability = 25
        }
        final = yes
    }
}