namespace = decentralization_mission

decentralization_mission.1 = {	#Nobles Seize Opportunity to claim land from serfs in [location]
	type = country_event

	title = decentralization_mission.1.title
	desc = decentralization_mission.1.desc

	immediate = {
		random_owned_location = {
			limit = {
				this != root.capital
			}
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {	#Allow them to claim it
		name = decentralization_mission.1.a

		scope:target_location = {
			add_location_modifier = {
				mode = add_and_extend
				modifier = decentralization_mission_nobility_owned_land
			}
			custom_tooltip = {
				text = mission_peasants_lose_satisfaction_mild
				every_pop = {
					limit = {
						owner ?= root
						pop_type = pop_type:peasants
					}
					add_pop_satisfaction = pop_satisfaction_mild_penalty
				}
			}
		}
	}

	option = {	#Restrict them
		name = decentralization_mission.1.b

		add_estate_satisfaction = {
			type = estate_type:nobles_estate
			value = estate_satisfaction_mild_penalty
		}
	}
}

decentralization_mission.2 = {	#Nobility Stronghold in [location]
	type = country_event

	title = decentralization_mission.2.title
	desc = decentralization_mission.2.desc

	immediate = {
		random_owned_location = {
			limit = {
				this != root.capital
			}
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {	#Great!
		name = decentralization_mission.2.a

		add_estate_satisfaction = {
			type = estate_type:nobles_estate
			value = estate_satisfaction_mild_bonus
		}
	}
}

decentralization_mission.3 = {	#Nobility Stronghold in [location]
	type = country_event

	title = decentralization_mission.3.title
	desc = decentralization_mission.3.desc

	immediate = {
		random_owned_location = {
			limit = {
				this != root.capital
			}
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {	#Great!
		name = decentralization_mission.3.a

		scope:target_location = {
			add_location_modifier = {
				mode = add_and_extend
				modifier = decentralization_mission_nobility_burgher_trade_haven
			}
		}

		add_estate_satisfaction = {
			type = estate_type:burghers_estate
			value = estate_satisfaction_mild_bonus
		}
	}
}

decentralization_mission.4 = {	#Burghers turn [location] into a place to avoid taxes
	type = country_event

	title = decentralization_mission.4.title
	desc = decentralization_mission.4.desc

	immediate = {
		random_owned_location = {
			limit = {
				this != root.capital
			}
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {	#Wtf?
		name = decentralization_mission.4.a

		scope:target_location = {
			add_location_modifier = {
				mode = add_and_extend
				modifier = decentralization_mission_nobility_burgher_tax_haven
			}

			change_control = control_extreme_penalty
		}
	}
}

decentralization_mission.5 = {	#Clerical influence in [location]
	type = country_event

	title = decentralization_mission.5.title
	desc = decentralization_mission.5.desc

	immediate = {
		random_owned_location = {
			limit = {
				this != root.capital
			}
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {	#Wtf?
		name = decentralization_mission.5.a

		change_societal_value = {
			type = spiritualist_vs_humanist
			value = societal_value_move_to_left
		}

		add_estate_satisfaction = {
			type = estate_type:clergy_estate
			value = estate_satisfaction_mild_bonus
		}
	}

	option = {	#No
		name = decentralization_mission.5.b

		add_estate_satisfaction = {
			type = estate_type:clergy_estate
			value = estate_satisfaction_mild_penalty
		}
	}
}

decentralization_mission.6 = {	#The devout of [location]
	type = country_event

	title = decentralization_mission.6.title
	desc = decentralization_mission.6.desc

	immediate = {
		random_owned_location = {
			limit = {
				this != root.capital
			}
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {	#Nice
		name = decentralization_mission.6.a

		scope:target_location = {
			custom_tooltip = {
				text = mission_every_pop_becomes_satisfied_extreme
				every_pop = {
					limit = {
						owner ?= root
					}
					add_pop_satisfaction = estate_satisfaction_extreme_bonus
				}
			}
		}
	}
}

decentralization_mission.7 = {	#Peasants claim the lands of [location]
	type = country_event

	title = decentralization_mission.7.title
	desc = decentralization_mission.7.desc

	trigger = {
		societal_value:serfdom_vs_free_subjects >= -20
	}

	immediate = {
		random_owned_location = {
			limit = {
				this != root.capital
			}
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {	#Nice
		name = decentralization_mission.7.a

		scope:target_location = {
			add_location_modifier = {
				modifier = mission_decentralization_peasant_control
				years = 10
			}
		}
	}
}

decentralization_mission.8 = {	#Poacher gains in [location]
	type = country_event

	title = decentralization_mission.8.title
	desc = decentralization_mission.8.desc

	trigger = {
		societal_value:serfdom_vs_free_subjects >= -20
	}

	immediate = {
		random_owned_location = {
			limit = {
				this != root.capital
			}
			save_scope_as = target_location
		}

		ruler_or_regent ?= {
			save_scope_as = target_character
		}
	}

	option = {	#Nice
		name = decentralization_mission.8.a

		add_gold = {
			value = scope:target_location.development
			multiply = 2
			max = 100
		}
	}
}