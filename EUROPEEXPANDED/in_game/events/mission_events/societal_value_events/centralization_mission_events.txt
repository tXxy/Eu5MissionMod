namespace = centralization_missions

centralization_missions.1 = {	#Choose approach of centralization of the area
	type = country_event

	title = centralization_missions.1.title
	desc = centralization_missions.1.desc

	immediate = {
        var:mission_centralization_location = {
			save_scope_as = target_location
			area = {
				save_scope_as = target_area
			}
        }
	}

	option = {	#Centralize it to [location]
		name = centralization_missions.1.a

		scope:target_location = {
			add_location_modifier = {
				mode = add_and_extend
				modifier = centralization_mission_centralized_into_one_location
			}
		}

		set_variable = {
			name = mission_centralization_location
			value = scope:target_location
		}

		#remove_variable = area_location	TODO: caused error log.
	}

	option = {	#Put effort everywhere
		name = centralization_missions.1.b

		#remove_variable = area_location
	}
}